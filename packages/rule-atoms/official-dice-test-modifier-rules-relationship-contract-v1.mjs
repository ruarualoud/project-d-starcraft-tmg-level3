import {
  OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ATOM_IDS,
  OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ID,
  OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_VERSION,
} from "./official-dice-test-modifier-rules-executor-v1.mjs";
import { createOfficialPlayerControlRelationshipRulesRelationshipExtensionV1 } from
  "./official-player-control-relationship-rules-relationship-contract-v1.mjs";

export const OFFICIAL_DICE_TEST_MODIFIER_RULES_RELATIONSHIP_SCOPE_ID =
  "ticket-11-slice-89-dice-test-modifier-rules";

const ID = Object.freeze({
  source: "state_field:officialDevelopmentTrancheSourceLockAudit",
  gameplay: "state_field:officialGameplayDataBundle",
  sourceData: "state_field:officialDiceTestModifierDataBundle",
  mode: "state_field:rulesProcedureMode",
  activeSide: "state_field:activeSideKey",
  players: "state_field:players",
  pending: "state_field:pendingAction.diceTestModifierRules",
  history: "state_field:diceRulesHistory",
  result: "state_field:lastDiceTestModifierResolution",
  log: "state_field:log",
  choose: "action_variant:diceTestModifierRulesV1.chooseCertifiedPlan",
  initial: "action_variant:diceTestModifierRulesV1.resolveInitial",
  reroll: "action_variant:diceTestModifierRulesV1.reroll",
  keep: "action_variant:diceTestModifierRulesV1.keepOriginal",
  registry: "derived_value:diceTestModifierV1.modifierRegistry",
  signed: "derived_value:diceTestModifierV1.signedModifierDirection",
  target: "derived_value:diceTestModifierV1.finalTargetNumber",
  nullCapability: "derived_value:diceTestModifierV1.nullCapability",
  classification: "derived_value:diceTestModifierV1.testClassification",
  initialRoll: "derived_value:diceTestModifierV1.initialTestRoll",
  rerollGrant: "derived_value:diceTestModifierV1.rerollGrant",
  rerollDecision: "derived_value:diceTestModifierV1.postRollRerollDecision",
  finalRoll: "derived_value:diceTestModifierV1.finalTestRoll",
  generated: "derived_value:diceTestModifierV1.generatedValue",
  agreement: "derived_value:diceTestModifierV1.cockedDiceAgreement",
  invalid: "derived_value:diceTestModifierV1.invalidPhysicalDie",
  replacement: "derived_value:diceTestModifierV1.invalidDieReplacement",
  event: "state_event:dice_test_modifier_rules_resolved",
  sourceTest: "judge_test:dice-test-modifier-v1-source-lock",
  modifierTest: "judge_test:dice-test-modifier-v1-modifier-arithmetic",
  nullTest: "judge_test:dice-test-modifier-v1-null-capability",
  classificationTest: "judge_test:dice-test-modifier-v1-test-classification",
  rerollTest: "judge_test:dice-test-modifier-v1-reroll-lifecycle",
  generatedTest: "judge_test:dice-test-modifier-v1-generated-value",
  cockedTest: "judge_test:dice-test-modifier-v1-cocked-die",
  authorityTest: "judge_test:dice-test-modifier-v1-authority-replay",
  graphTest: "judge_test:dice-test-modifier-v1-relationship-negative-gap",
});

function fail(code) { throw new Error(code); }
function node(nodeId, kind, label) {
  return { nodeId, kind, label, provenance: "ticket-11-slice-89" };
}
function edge(from, relationship, to, provenance) {
  return { scopeId: OFFICIAL_DICE_TEST_MODIFIER_RULES_RELATIONSHIP_SCOPE_ID,
    from, relationship, to, provenance };
}

export function createOfficialDiceTestModifierRulesRelationshipExtensionV1(
  input = {},
) {
  const catalogueHash = String(input.catalogueHash || "");
  const runtimeHash = String(input.runtimeHash || "");
  if (!/^[a-f0-9]{64}$/u.test(catalogueHash)
    || !/^[a-f0-9]{64}$/u.test(runtimeHash)) {
    fail("DICE_TEST_MODIFIER_RELEASE_INVALID");
  }
  const previous = createOfficialPlayerControlRelationshipRulesRelationshipExtensionV1({
    catalogueHash, runtimeHash,
  });
  const executor = `executor:${OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ID}`
    + `@${OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_VERSION}`;
  const reads = [ID.source, ID.gameplay, ID.sourceData, ID.mode, ID.activeSide,
    ID.players, ID.pending, ID.history, ID.result];
  const edges = [
    ...reads.map((target) => edge(executor, "reads", target,
      "dice_test_modifier:state_contract")),
    edge(executor, "exposes", ID.choose,
      "dice_test_modifier:certified_choices"),
    edge(ID.choose, "derives", ID.initial,
      "dice_test_modifier:instantiate"),
    edge(ID.sourceData, "derives", ID.registry,
      "dice_test_modifier:official_modifier_semantics"),
    edge(ID.registry, "derives", ID.signed,
      "dice_test_modifier:buff_debuff_modifier_direction"),
    edge(ID.signed, "derives", ID.target,
      "dice_test_modifier:different_named_source_sum"),
    edge(ID.registry, "derives", ID.nullCapability,
      "dice_test_modifier:null_cannot_gain_roll"),
    edge(ID.nullCapability, "derives", ID.target,
      "dice_test_modifier:roll_eligibility"),
    edge(ID.sourceData, "derives", ID.classification,
      "dice_test_modifier:characteristic_attribute_test"),
    edge(ID.target, "derives", ID.initialRoll,
      "dice_test_modifier:target_before_roll"),
    edge(ID.classification, "derives", ID.initialRoll,
      "dice_test_modifier:test_kind"),
    edge(ID.initial, "derives", ID.initialRoll,
      "dice_test_modifier:authority_chance_reveal"),
    edge(ID.sourceData, "derives", ID.rerollGrant,
      "dice_test_modifier:default_or_specified_scope"),
    edge(ID.initialRoll, "derives", ID.rerollDecision,
      "dice_test_modifier:result_visible_before_choice"),
    edge(ID.rerollGrant, "derives", ID.rerollDecision,
      "dice_test_modifier:maximum_selected_dice"),
    edge(ID.rerollDecision, "derives", ID.reroll,
      "dice_test_modifier:reroll_choice"),
    edge(ID.rerollDecision, "derives", ID.keep,
      "dice_test_modifier:keep_choice"),
    edge(ID.reroll, "derives", ID.finalRoll,
      "dice_test_modifier:replacement_even_when_worse"),
    edge(ID.keep, "derives", ID.finalRoll,
      "dice_test_modifier:retain_original"),
    edge(ID.sourceData, "derives", ID.generated,
      "dice_test_modifier:fixed_addition_not_test"),
    edge(ID.sourceData, "derives", ID.agreement,
      "dice_test_modifier:pregame_agreement"),
    edge(ID.agreement, "derives", ID.invalid,
      "dice_test_modifier:physical_die_validity"),
    edge(ID.invalid, "derives", ID.replacement,
      "dice_test_modifier:mandatory_reroll"),
    edge(ID.initialRoll, "derives", ID.event,
      "dice_test_modifier:initial_or_final_test"),
    edge(ID.finalRoll, "derives", ID.event,
      "dice_test_modifier:reroll_final"),
    edge(ID.generated, "derives", ID.event,
      "dice_test_modifier:generated_value"),
    edge(ID.replacement, "derives", ID.event,
      "dice_test_modifier:invalid_die_replacement"),
    edge(ID.target, "derives", ID.event,
      "dice_test_modifier:modifier_query"),
    edge(ID.event, "writes", ID.pending,
      "dice_test_modifier:pending_stage_or_clear"),
    edge(ID.event, "writes", ID.history,
      "dice_test_modifier:history"),
    edge(ID.event, "writes", ID.result,
      "dice_test_modifier:last_resolution"),
    edge(ID.event, "writes", ID.log,
      "dice_test_modifier:log"),
    edge(ID.source, "verified_by", ID.sourceTest,
      "dice_test_modifier:source_judge"),
    edge(ID.target, "verified_by", ID.modifierTest,
      "dice_test_modifier:modifier_judge"),
    edge(ID.nullCapability, "verified_by", ID.nullTest,
      "dice_test_modifier:null_judge"),
    edge(ID.classification, "verified_by", ID.classificationTest,
      "dice_test_modifier:classification_judge"),
    edge(ID.rerollDecision, "verified_by", ID.rerollTest,
      "dice_test_modifier:reroll_judge"),
    edge(ID.finalRoll, "verified_by", ID.rerollTest,
      "dice_test_modifier:replacement_judge"),
    edge(ID.generated, "verified_by", ID.generatedTest,
      "dice_test_modifier:generated_judge"),
    edge(ID.invalid, "verified_by", ID.cockedTest,
      "dice_test_modifier:cocked_judge"),
    edge(executor, "verified_by", ID.authorityTest,
      "dice_test_modifier:authority"),
    edge(executor, "verified_by", ID.graphTest,
      "dice_test_modifier:relationship"),
    ...reads.map((source) => edge(source, "invalidates", ID.initial,
      "dice_test_modifier:stale")),
    ...reads.map((source) => edge(source, "invalidates", ID.reroll,
      "dice_test_modifier:stale_reroll")),
  ];
  const additions = [
    node(ID.sourceData, "state_field", "Official dice/test/modifier source bundle"),
    node(ID.pending, "state_field", "Dice/test/modifier pending"),
    node(ID.history, "state_field", "Dice rules history"),
    node(ID.result, "state_field", "Last dice/test/modifier resolution"),
    node(ID.choose, "action_variant", "Choose certified dice rules plan"),
    node(ID.initial, "action_variant", "Resolve initial dice rules plan"),
    node(ID.reroll, "action_variant", "Re-roll selected dice"),
    node(ID.keep, "action_variant", "Keep original dice"),
    node(ID.registry, "derived_value", "Rules-owned modifier registry"),
    node(ID.signed, "derived_value", "Signed modifier direction"),
    node(ID.target, "derived_value", "Final bounded target number"),
    node(ID.nullCapability, "derived_value", "Null capability roll eligibility"),
    node(ID.classification, "derived_value", "Characteristic or attribute test"),
    node(ID.initialRoll, "derived_value", "Initial test result"),
    node(ID.rerollGrant, "derived_value", "Rules-owned re-roll grant"),
    node(ID.rerollDecision, "derived_value", "Post-result re-roll decision"),
    node(ID.finalRoll, "derived_value", "Final replaced-or-kept test result"),
    node(ID.generated, "derived_value", "Generated fixed-addition value"),
    node(ID.agreement, "derived_value", "Pre-game cocked dice agreement"),
    node(ID.invalid, "derived_value", "Invalid physical die classification"),
    node(ID.replacement, "derived_value", "Invalid die replacement result"),
    node(ID.event, "state_event", "Dice/test/modifier rules resolved"),
    node(ID.sourceTest, "judge_test", "Pinned Core source Judge"),
    node(ID.modifierTest, "judge_test", "Modifier arithmetic Judge"),
    node(ID.nullTest, "judge_test", "Null capability Judge"),
    node(ID.classificationTest, "judge_test", "Test classification Judge"),
    node(ID.rerollTest, "judge_test", "Re-roll lifecycle Judge"),
    node(ID.generatedTest, "judge_test", "Generated value Judge"),
    node(ID.cockedTest, "judge_test", "Cocked dice Judge"),
    node(ID.authorityTest, "judge_test", "Authority chance/replay Judge"),
    node(ID.graphTest, "judge_test", "Relationship negative-gap Judge"),
  ];
  const previousIds = new Set(previous.nodes.map((entry) => entry.nodeId));
  return {
    nodes: [...previous.nodes,
      ...additions.filter((entry) => !previousIds.has(entry.nodeId))],
    edges: [...previous.edges, ...edges],
    executorLineages: [...previous.executorLineages, {
      executorId: OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ID,
      ruleAtomIds: [...OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ATOM_IDS],
      provenance: "runtime_action_lineage:dice_test_modifier_rules_v1",
    }],
    declaredStateContractExecutorIds: [
      ...previous.declaredStateContractExecutorIds,
      OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ID,
    ],
    coverageScopes: [...previous.coverageScopes, {
      scopeId: OFFICIAL_DICE_TEST_MODIFIER_RULES_RELATIONSHIP_SCOPE_ID,
      executorId: OFFICIAL_DICE_TEST_MODIFIER_RULES_EXECUTOR_ID,
      requiredNodeIds: [...new Set([executor, ...reads, ID.log, ID.choose,
        ID.initial, ID.reroll, ID.keep, ID.registry, ID.signed, ID.target,
        ID.nullCapability, ID.classification, ID.initialRoll, ID.rerollGrant,
        ID.rerollDecision, ID.finalRoll, ID.generated, ID.agreement, ID.invalid,
        ID.replacement, ID.event, ID.sourceTest, ID.modifierTest, ID.nullTest,
        ID.classificationTest, ID.rerollTest, ID.generatedTest, ID.cockedTest,
        ID.authorityTest, ID.graphTest])],
      requiredEdges: edges,
      requiredPaths: [
        { from: ID.sourceData, to: ID.event,
          relationships: ["derives"], maxDepth: 12 },
        { from: ID.initialRoll, to: ID.event,
          relationships: ["derives"], maxDepth: 8 },
        { from: ID.rerollGrant, to: ID.event,
          relationships: ["derives"], maxDepth: 10 },
      ],
      forbiddenPaths: [],
      evidenceTestNodeIds: [ID.sourceTest, ID.modifierTest, ID.nullTest,
        ID.classificationTest, ID.rerollTest, ID.generatedTest, ID.cockedTest,
        ID.authorityTest, ID.graphTest],
    }],
  };
}
