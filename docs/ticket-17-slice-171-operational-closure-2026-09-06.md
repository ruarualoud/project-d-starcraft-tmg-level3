# Ticket 17 / Slice 171 — operational closure

Ticket17: 9/9 complete. Project:16/22 complete. Ticket14 physical-device
acceptance remains deferred. First-five runtime acceptance remains0/5.

## Executed change

The reproducible Direct-loop regression accepted a finish response after its
configured wall deadline. Both arms now share a monotonic session deadline;
its AbortSignal reaches the accounted model and isolated Provider Worker.
Late results cannot be accepted and another formatting attempt cannot begin
after expiry. Cancellation awaits durable settlement rather than abandoning a
billable promise or closing its database underneath it. The original test
failed with `Missing expected rejection`; the corrected test passes.

DSH execution now uses a content-hashed projection of the unchanged pinned
0.1.1-rc.2 installation. Only source maps and TypeScript declarations are
removed from staging. Runtime JS/TS, native libraries, package metadata,
assets, directories and symlinks are retained; a symlink referencing an
omitted file rejects the projection. Both original and derived manifests are
verified from actual bytes before use. OS isolation is unchanged, with no
repository mount, new network access or publication capability.

The full parent remains35,962 entries/204,121,239 bytes. The execution
projection has21,329 entries/141,313,731 bytes, omitting14,633 non-runtime
files. Same-host real DSH read/error-recovery/probe/finish engineering test:
48,265ms before,23,917ms after. This is one descriptive local timing pair,
not a universal throughput or strategy-quality claim. One-time preparation
still scans/builds the projection; there is no unverified cache shortcut.
Generated projection directories are explicitly ignored rebuildable assets.

## Verification actually run

- `npm run verify:ticket-17-slice-170` completed successfully, including the
  Ticket15/16 predecessor sequence, real browser/injected Provider checks,
  Ticket16 live-receipt revalidation, all Ticket17 isolation/runtime/broker
  gates and frozen Slice170 production-result reconstruction. No paid call.
- After the staging change, the focused production gate passed18/18 again,
  executing actual pinned DSH with real tool traffic through unchanged OS
  isolation. Readiness `c0c108fad7391caa9fa50f8b98175e0e22d8b3db46283099ce00602eb6f0e964`.
- The deadline/projection regression verifies cancellation, accounting drain,
  successful completion and the exact omission boundary.
- Earlier current semantic87, disposition9, legacy-entrypoint hold9 and
  Provider40 gates passed. Frozen pilot closure remains
  `82557af070d05f8cf82c5dd1f8be30740969154206d335b8572f61847fe2d5d1`.

Original pilot20/20 chapters, raw28/36 versus23/36 and clarified35/36 per
arm remain unchanged and quarantined. No paid call, source update, room
mutation, Skill publication, memory write, self-play or training promotion
was performed by this slice. Cumulative known-token lower bound4,556,327;
known-usage estimate¥7.535144; plus historical uncertainty reserve¥28.961350
=¥36.496494 estimated/reserved, not an invoice.

## Handoff

Ticket18 Slices172–179 are fixed in the linked first-five/evolution plan.
Slice172 replaces the pilot's partial reading denominator and fixed claim
counts with full frozen-source reading packets, source-completeness review
and bounded local additions/replacements. Later slices must actually generate
and test the five Skills, then prove room use and postgame version evolution.
Production container/microVM replacement for deprecated macOS sandbox-exec
remains Ticket21; no production deployment or DSH benefit is claimed here.

Ctx2Skill/Harness used; targetGames=[starcraft-tmg]; roles=Tutor, Generator,
independent Reviewers, Editor, held-out Student; real tools=read/query/probe;
Cross-Time=mechanics journal replay only; promotions=[]; gaps=first-five
quality/arena/registry and postgame/SkillOpt acceptance. DSH remains offline.
