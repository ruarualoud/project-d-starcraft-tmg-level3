# Ticket18 / Slice172 — reusable full-source packet workflow

Ticket18:1/8 slices complete,173 next. First-five acceptance:0/5.
No paid model call or official-source refresh in this slice.

The source plan now assigns every eligible core/FAQ passage exactly once:
220 core rows +68 FAQ rows =288 rows,364 spans,37 internal reading packets.
Two official placeholder rows stay explicitly quarantined. All1,163 current
RuleAtom identities remain in the plan, including114 display-only atoms.
Reading coverage is not a claim of complete semantic or executable coverage.
The37 packets belong to ONE overall-rules Skill, not37 published Skills.

Every packet receives full frozen FAQ context so current FAQ corrections can
override conflicting core prose during generation and review. Prompt-exposed
source reads and actual tool reads are recorded separately. Runtime source
addresses and quotes are materialized by the host; models cannot write quotes,
source hashes or execution results. Current paid generation targets Terran
Armed Forces and Zerg Swarm only after the overall dependency passes.

The reusable workflow is Tutor→Generator→independent supportive/adversarial
reviews→bounded localized Editor→fresh re-review. Review has two independent
denominators: each rendered statement and each assigned source passage.
True individual claims cannot conceal omitted source rules. No minimum number
of cautions or strategies forces filler. Negative/disagreeing review results
require repair or quarantine, not automatic majority approval.

Local patches bind the exact parent hash; only flagged claim IDs may change,
and additions must address identified missing passages. Empty, unchanged,
unauthorized, stale or fabricated-citation patches reject. Schema repairs
are bounded and retain failed outputs/read provenance; semantic repair has a
fixed two-revision limit. SQLite completed stages survive restart; an
ambiguous paid attempt still cannot be retried automatically. Cross-recipe
production recovery remains a later scheduler concern, not implied here.

Seven focused check groups passed in
`scripts/verify-ticket-18-packet-production-v1.mjs`: exact source/atom/five-Skill
denominators; missing/duplicate/reordered packet rejection; complete rendered
claim inventory; independent source-completeness veto; omission-only patching;
read/probe/write capability boundaries; and actual workflow orchestration with
durable checkpoint/restart. The model port is explicitly injected in these
tests, and they do not prove real-model semantic quality or whole-game play.

Ctx2Skill and Harness used; targetGames=[starcraft-tmg]; sources/skills scoped
to the frozen current game; runtime tools=read/query/development-probe only;
promotions=[]; roomReplayPerformed=false; trainingTruth=false. Live packet
production, held-out application, package synthesis, first-five registry/arena
acceptance and postgame/SkillOpt evolution remain the following slices.
