# Ticket 18 / Slice 174：续跑完整性与解析分流

2026-09-09。工程修复记录，不是 Skill 发布或对战验收。

## 两个实际故障

1. `faction-v1-f3c43a4f1ec255cbc9ea` 中 Terran 重发 tutor，随后 question-tree 输出截断。旧 tutor 与新 tutor 的输入 hash 相同。续跑器只从直接父轮次提取已落盘的角色结果：Terran 被隔离跳过之后，其祖先结果逐轮消失于可复用索引。这里不能用增加输出上限掩盖重复初始化。
2. 同轮 Zerg `structured-2558946a2c52a16c26dcdaac54f1539d416207cdc5f8eba2` 为语法错误。认证原始响应后，唯一合法解释仅删除一个多余的 `}`，不改标量、不删字段、不补内容，且本地 schema 已通过。旧入口仍调用要求 schema 不合法的纠错准备器，导致系统自己拒绝有效的解析结果。

## 修复合同

- 跨已认证祖先建立原始角色产物索引，消费时仍要求角色 ID、输入 hash、产物 hash 完全一致。不继承语义审核结论，不复制请求或重置费用。
- 显式恢复 Terran 至 `27ef94cd6f32462ec36a` 的一致产物分支；仅排除 f3 同输入冗余 tutor 的候选选择。新 tutor、截断响应及付款记录都保留。f3 之后的有效新结果仍优先，不永久锁死旧分支。
- 提示来源按实际产物所属轮次回溯；独立读取器按真实祖先位置审核，不能把祖先行伪装成直接父轮次的行。
- 共享 canonical 名称的不同来源版本只在路由名单去重。原始产物及输入不合并，旧模型输出不被标成新模型输出。
- 解析恢复后先做本地 schema 分类：合法值进入 DSH 原样物化与 Host 映射；非法值进入有界字段纠错。语法修复不等于来源通过，更不等于策略通过。
- 支持字段纠错响应再次需要语法恢复的组合。两个原始失败及其凭据分别保留，不制造原生成功回执，不重发原请求。

## 验证与尚未完成的验收

联合验证 `e08d199ee1f6bd9786cf518c1f95eb89503112d5dbf3bcfc5772eb4b610b4846`：38 项、2 次真实 DSH 会话、0 新 Provider 请求。实际 Zerg 响应已通过正式角色工厂、SQLite 重开和完整独立消费者；另有明确标注的组合故障注入，不能当作真实模型产出。

实际索引找回 432 个原始角色结果：Terran 348、Zerg 84。这不是 432 个 Skill，也不是新增完成章节。实际祖先审计保留全部付款记录。包装器输入测试使用单独 fixture；不能把 fixture 输入 hash 当作真实 tutor 的请求复现。

关联回归及正式 f3 续跑仍须通过。当前基础包离线验收 1/5，对战验收 0/5；Terran 历史写作章节 6/7，Zerg 2/8。禁止仅凭本文件、组件通过或 preflight 成功结案。

## 连续生产期间的后续风险

认证语法恢复目前每次读取均依赖原始密文 TTL；Terran 旧 target.0 的原始密文约于 9 月 9 日 17:39 CST 到期。这是暂存失败响应的保留期限，不应被误认为最终 Skill 的长期可用性证明。不得延长或绕过旧 TTL。需审查“恢复完成的长期证明”与“失败原文短期留存”的职责边界；未完成前不得声称恢复产物可永久重放。

beta 模型退场的自动主入口降级、后续消费者使用的实际模型绑定，以及链预算接近上限时保留账本的续额机制仍须跟进。当前实际累计约 ¥175.570997；下一次成本提醒 ¥200。真实 API 402 仍要求停止所有开发。

### 恢复证明的独立验证（16:42 CST）

`recovery-attestation-s2xpyv/report.json` 的 27 项测试通过，报告 hash 为 `0e85cb6eeb08ecbdfe89c3ed278a76b8dd72155ea31263cdecf989e0c4336c6b`。对实际 Zerg 语法恢复和 Terran target.0 各签发一份 Ed25519 证明。签发时重新认证原始响应，验证时固定外部公钥并核对原请求、所属轮次、失败记录及付费账本。未来时钟测试不读取已过期原文；改动内容、公钥、请求或费用均被拒收。首次测试发现 `issue` 报告用量与 `attempt` 账本用量字段名不同，已按精确字段映射修复，未改实际账本。

这是本地开发 Host 显式固定公钥的证明，不是远端生产签名权；`productionMainWired=false`。主生产和最终消费尚未采用该信任合同，不能据此宣称旧响应 TTL 问题已全部解决。原文保留期不变，来源/策略/对战验收不继承。

17:00 CST 增补独立 registry 读取器：只读取外部明确固定 hash 的清单及公钥，不扫描目录发现信任来源。重建的完整请求、实际 owner/issue/attempt 仍逐次核验。`recovery-attestation-registry-PBAQEn/report.json` 通过 13 项；实际 Zerg 恢复请求在未来时钟下完成两次独立打开与验证，清单含上述两条归档。报告 `7468dad319e39913bfca7c44abb30351e0e53c48bbefd2c912c9816377cc1f6f`，清单 `880963046ee1e64295e402cf0dff3ca08f993760ca789b95bd62b1a3f4406d10`。原始账本未写，0 Provider、0 DSH；仍未接正式入口，不能算新增 Skill 或完整生产恢复。

本次使用 offline-skill-evolution、ctx2skill-rule-skill-loop、agentic-harness-evolution-loop 的要求：工程输出、策略语义验收、对战效果及训练真值必须分离；跨阶段来源和失败证据须可独立重建。`ctx2skillLoopUsed=true`，`harnessLoopUsed=true`，game scope=`starcraft-tmg`；未作运行时或记忆晋升。

## bf7 正式运行结果与合同修复（17:43 CST）

正式轮次 `faction-v1-bf7beab1dccb73982637` 已终止，后续必须从它续跑。前述两项修复已在实际生产生效：Terran 复用原六章，没有再次生成 tutor；混合审核的三个片段已收齐并进入后续审核。Zerg 的 f3 语法响应零新 Provider 恢复成功。新增 14 次真实调用、1,696,170 tokens、估算 ¥5.150436，但没有新增审核完成章节。全局累计 183,059,825 个已知 tokens、估算及未知预留 ¥180.721433；不等于账单。

本轮暴露两个不同层次的合同问题，不能都交给模型重新生成：

- Zerg adversarial.1.4 的 Host coverage 任务集为空，模型省略 `coverage`。旧字段纠错指令禁止空占位，反而诱导产生不存在的 coverage 槽位。新合同只在已认证任务集确实为空且字段缺失时补 `[]`，不覆盖已提供内容，不编造肯定的来源判断。两次已付费的错误补写保留。
- Terran adversarial.0.4 多出 `recommendationSlots_note="host-owned"`。新提取器不按字段名穷举例外；识别极小的行政归属标记语法并逐字保留到附注，不赋予任何权威。任意规则正文、否定判断或 Host 控制字段仍须审查，不能静默删除。

两份真实完整请求已重建。组件 40 项、运行时 16 项及两次真实 DSH、正式 lane 与完整独立回放 25 项及两次真实 DSH 均通过，零新增 Provider。接线报告 `2ad169a9a04f58b53952d8ff82ffbb4d0020f006a718a83770e5aaa17dff6ea2` 为签名凭证接入前的版本；接入后的关联验证仍在执行。这里的 callback 测试复用了实际失败记录，未注入一次新的传输，不能把它表述为已经验证新的实际付费失败。

原文 TTL 已与长期证明分离：新增显式本地 Host 信任配置，固定先前签发的 registry hash，不从目录或模型内容发现公钥。读取旧 proof 时核对完整请求、原 owner/issue/attempt 和 Ed25519 签名，保持旧 proof hash、旧模型标识及费用不变；不再打开该已到期原文。此认证实现升级不修改旧规则或旧产物内容，也不继承来源/策略通过结论。当前仅接入 opening-fence 读取路径；其他恢复类型仍须各自接入、验证。

下一轮预算预估将跨越 ¥200，已在本次进度消息提前通知用户；沿用其剩余 Skill 生成授权，必须写入新档位通知凭证并保留完整消耗，不允许重置成本。基础包仍 1/5 离线、0/5 对战，Terran 6/7、Zerg 2/8 写作单元；后续继续实际生成和独立评估，不在工程测试通过时结案。

### 用户后续预算指令覆盖（17:55 CST）

用户明确要求预算从 0 开始、已有预算释放。因此上述下一档 ¥200 的安排已被覆盖：新周期以 `budget-epoch-2026-09-09-reset-v1.json` 为界，从 ¥0 开始，每 ¥100 提醒。历史账本和未知费用保留，不宣称退款。新周期预留 ¥100 安全额度；底层仍用“历史基线 + 新额度”的累积限额保持并发原子扣减，只在展示和本期费用中减去固定历史基线。真实账本基线校验、旧限额解除、继承期间不能擅自再次归零等 27 项测试通过，旧预算 59 项回归通过。此改动不释放未对账请求的重复发送隔离，也不影响实际 API 402 全局停止规则。
