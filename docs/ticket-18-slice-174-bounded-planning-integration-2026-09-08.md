# Ticket 18 / Slice 174 — bounded planning integration

Source boundary: the existing frozen official StarCraft TMG input and FAQ remain unchanged. No source refresh, credential disclosure, new dependency or Codex subagent.

## Decision and actual repair

The user permits increasing output capacity or batching. The actual c7 Zerg Proposer exhausted an8192 output limit; this establishes truncation, not that large token budgets cause hallucinations. Keep8192 for new outline/two-advice writing. Replace the large Proposer note envelope with a dedicated short-plan schema: at most two indexed plans per response, each600 characters, plus at most two short uncertainties. This is an intermediate planning bound, not a limit on final strategy content.

Every batch retains the complete frozen sources, overall dependency, original question tree/questions, complete answers and latest Judge, including negatives and the source correction receipt. Previously completed batch text also remains available. Every original question and every assigned source absent from those questions receives a writing obligation. Only a complete, losslessly assembled set reaches the outline. Truncation, missing indices and repeated indices cannot produce an assembly. No incomplete prefix or automatic full-role retry is used.

The workflow calls the native Schema/DSH path for each child. A derived assembly has no paid-role identity. The consumer independently reconstructs child inputs and actual4096 Provider request bodies; altered targets/Judge or missing bindings fail. Historical contracts remain unchanged when the new binding is absent.

Two related semantic fixes are wired separately:

- The exact known wrong Unique assertion is checked independently of its changing paragraph prefix. A host-authored correction preserves every other field, then requires both whole-section review routes at a new revision. A fresh negative remains blocking. The independent consumer rejects the known assertion even if model reviews agreed.
- A newly assembled plan gives outline/items a new generation identity. The c7 seventh chapter's previously completed Proposer/outline cannot masquerade as an accepted chapter. The three omitted assigned cards—Dropship, Factory and Supply Depot—become explicit planning jobs. The actual old no-change outline repair still fails; citation coverage is not semantic acceptance.

The explicit migration binds the actual c7 paid truncation, chapter-six result, chapter-seven failed outline/no-change response, code and readiness receipts. It freezes only the six prior Terran writing plans, not their acceptance. The existing accounting/start/source/model boundaries remain inherited. The runner and its dry preflight use the same route and a separately probed short-plan contract.

## Verification status

New focused proofs initially passed:

- Short-plan unit35; exact Unique assertion unit19.
- Native42: three real isolated DSH sessions with a simulated Provider, complete-context and paid-wire consumer replay, cache reuse, missing output/truncation/payment-stop tests. No real Provider billing.
- Workflow26: actual c7 chapter-six text and chapter-seven scope failures, real Terran tree/challenger with injected surrounding roles,16 fresh review batches across positive/negative branches. The real eight-question Terran card axis plus three omitted sources yields five short-plan batches. Zerg's six-question first chapter yields three.
- Migration18: actual database origin, incomplete/removed bindings, unrelated origin, changed code and attempts to freeze the failed seventh plan are rejected.

The final native guard excludes this new plan contract from legacy reference-set recovery. The affected proofs were rerun successfully: native42/3DSH `cda06fb0…`, workflow26 `168e6f48…`, migration18 `b1002fd3…`. The actual c7 failed8192 request, including its real fresh Judge, was also rebuilt unchanged (`4c3c9759…`).

Related old gates have passed workflow77, continuation40, native44, budget59, parallel/catalogue, editor5/review19, unit16/unit-workflow8, field20/phase20/targeted28, review transaction, two DSH-context sessions, source-workflow8, observed-source26, capacity29, target-ID25 and metadata14. Of36 selected startup gates,33 currently match the code. Session79125 alone remains active: answer-completion wiring, then Teach-uncertainty and review-focus-capacity; the other sessions ended successfully. Main preflight/live have not started. These are engineering proofs, not generated faction Skills or complete-game effectiveness.

Follow-up: session79125 ended successfully with answer-completion16/3DSH, Teach-uncertainty33/1DSH and focus-capacity25. All36 selected startup gates/140 files now match. Consumer preparation also passed the four affected checks, with no paid evaluation. The unique current process is zero-paid main preflight59296/PID52876 from c7; live production has not started. This supersedes the waiting state above, not the historical paid results.

Preflight59296 subsequently exited0, ready. Recipe hash is `c96a15b1a09894a777f7df39dd94020e05682e2eb2ec24062aa282de0087239c`;299 prior roles are reusable. The first uncached role is Terran chapter-six `review-target-batch-v1.supportive.3.0`, proving that the new correction leads to a fresh review, not inherited acceptance. The faction-only lineage inherits438 calls/91,073,308tokens/¥65.944433; this is not the cumulative whole-project cost. A unique live command from c7 is now running as session76414/PID60161, initially performing the same dependency/history checks. No new paid role or accepted chapter is claimed at this checkpoint. Production source code stays fixed while this process runs.

## Progress and cost

Project16/22 tickets; Ticket18 2/8 slices. Current174 is two faction packs;175 directed matchups;176 actual spatial arena;177 replay/reflection;178 versioned upgrades/regression/rollback;179 aggregate acceptance.

Formal offline five-pack1/5; runtime0/5. Terran workflow6/7, but independently cleared of the current known error remains5/7 until chapter six is actually corrected and rereviewed. Zerg0/8. Cumulative known API tokens161,595,962; estimated/reserved cost¥132.250695, unchanged in this engineering round. Next cost notice¥200; a real balance failure stops all development.

## Skill-loop report

- ctx2skillLoopUsed: true; targetGames: starcraft-tmg; roleRoutes: rule_skill_builder.
- skillsRead: frozen overall dependency, faction inputs, actual paid c7 intermediates and source-backed counterexamples.
- skillsGenerated: no new accepted Skill; judgeTestsRun: focused engineering cases above.
- crossTimeReplayResult: new native child reconstruction passes; full runner continuation/preflight remains pending.
- promotions: none; blocks: both faction packs still unqualified.
- remainingRuleGaps: semantic/source correctness beyond known counterexamples, independent strategy/application evaluation, actual spatial gameplay.
- harnessLoopUsed: true; promptPackRoutes: offline rule_skill_builder; harnessToolsCalled: isolated DSH/structured Provider Adapter with injected Provider in tests.
- uiTraceEvidence: none; agentDecisionEvidence: planning/correction/negative-preservation fixtures only; memoryTraceEvidence: none; trainingTraceCandidates: none.
- rollbackOrDemotionRules: old paid artifacts and profiles stay frozen; missing dependencies or changed bindings quarantine; no unsupported acceptance or training truth.
- userVisibleChecks: no claim that larger output or model consensus prevents hallucinations; final Skill text remains independent of planning-note limits.
