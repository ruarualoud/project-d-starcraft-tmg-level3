# StarCraft TMG Level-3 implementation plan

Date: 2026-08-24
Status: active long-task implementation plan
Wayfinder: [StarCraft TMG Level-3 Platform Wayfinder](../../.scratch/starcraft-tmg-level3-platform/map.md)

Fixed development root: `starcraft-tmg-level3/`. All new product code, contracts, verifiers and generated evidence stay below this directory. Repository-root StarCraft implementations are read-only Adapter inputs until an explicit integration gate authorizes a bounded change.

## Product outcome

Evolve the existing StarCraft TMG product and Battle Lab into one Level-3 platform whose Web and App clients share authoritative game contracts, official-source data and translations, room/replay state, direct-Provider role Agents, DSH-backed offline Skill generation, and player-view MuZero-compatible evidence.

The current UI is an experience asset, not a state authority. Rules, Referee, Agent/Skill, and Learning claims remain separate.

## Fixed decisions

- Recover the Expo product source and keep Battle Lab as the developer/referee/Agent-observability surface; both consume one domain SDK and service protocol.
- M1 has two mandatory lanes: an authoritative cross-client movement atom and a real room-connected Kerrigan Adjutant demo.
- Official StarCraft TMG sources own canonical game facts. Machine translations are provenance-bearing display sidecars.
- Kerrigan is the default product Adjutant role, not a canon Adjutant title. The first proposed edition is the rights-gated post-Zerus/pre-Legacy Primal Queen.
- Tutor, Opponent, Commentator, and Companion use isolated sessions, tools, memories, and visibility. Only the Opponent decision channel may submit a LegalSpace candidate.
- Online Agents use direct Providers with administrator profiles and session-memory-only BYOK. DSH is used only for offline Skill candidate generation.
- The first MuZero phase exports and validates data but does not train a learner or grant training truth.

## Authority planes

1. **Source plane**: raw official snapshots, source registry, hashes, diffs, normalized data, translation sidecars.
2. **Rules plane**: normalized state, LegalSpace, deterministic transition semantics, rule-source diagnostics.
3. **Referee plane**: revision/CAS, preview/apply parity, room capabilities, append-only receipts, public/private journal projections, replay.
4. **Agent and Skill plane**: CharacterPackage/worldbooks, direct Provider sessions, role tools/memory, offline DSH candidate generation, evaluation and promotion.
5. **Learning plane**: player-view observations, action encoding, episode lineage, leakage tests, round-trip export and independent eligibility.

## Milestones

### M0 — Baseline and source freeze

- Recover `ruarualoud/sc-tmg-app` branch `codex/starcraft-classic-army-builder` at `f07b3cb78ce6bf119bdc529cde41dbe91e00a61d`.
- Inventory Expo, Battle Lab, Web artifacts, APK artifacts, rules implementations, room clients, native dependencies, and translation stores.
- Seal official source/data manifests and exactness policy.

### M1 — Dual vertical slice

Execution lane:

- LegalSpace exposes the complete union of exact finite actions and parameter domains; movement is a rules-owned path domain and search suggestions remain non-authoritative.
- Preview binds the signed MatchBinding, room/state revision, state and LegalSpace hashes, canonical proposal, explicit GameClock, events, confirmation policy, optional ChanceTicket commitment, and post-state hash with an HMAC seal.
- Apply requires the same sealed preview, expected revisions, server-owned SeatGrant, current fenced ControlLease, idempotency identity, and any required human confirmation, then appends an Ed25519-signed receipt.
- A replay resolves the exact frozen dependency set and reproduces the same signed receipt/state chain or enters explicit quarantine; it never substitutes current rules.
- Web and App consume the same service contract.

Adjutant lane:

- A versioned Kerrigan CharacterPackage and worldbook are loaded through a direct Provider session.
- The Adjutant reads the same room projection and LegalSpace used by the client.
- Tutor, Commentator, and Companion remain read-only; Opponent may propose only a returned candidate and must use preview/apply.
- The trace records prompt route, tools, rule/Skill refs, memory refs, versions, decision alternatives, confirmation, result, and training ineligibility.

### M2 — Authoritative H-H room

- Durable room state, seat capabilities, CAS, append-only public/private journals, reconnect/recovery, deterministic replay, and Web/App parity.

### M3 — Complete role-Agent product

- Mature character/worldbook import and export, era/spoiler selection, four isolated modes, direct Provider profiles, BYOK, memory projections, rights fallback, observability and rollback.

### M4 — Official data and translation

- Versioned official ingestion, diff/review workflow, executable exactness coverage, translation providers/glossary/corrections, source indicators and offline cache.
- Current unit/card/points/mission/deployment values come only from the newest contract-verified official Command Center snapshot available at room creation. Each room freezes that snapshot; P2P PDFs are historical/cross-check inputs and repository-local values are never a production fallback.

### M5 — Offline Skill production

- Scheduler-owned DAG, leases, fencing, WAL, budgets and attempts; pinned isolated DSH Adapter plus direct-provider control; CandidateSkillBundle/RunReceipt; fact, held-out, replay and administrator promotion gates.

### M6 — H-A/A-A and self-play

- Frozen seat/Provider/Skill/rules/data bindings, finite opponent pools, pause/resume, failure accounting, experiment cells, safe traces and controlled evidence denominators.

### M7 — MuZero-compatible trajectory export

- Player-view observations, legal action encoding, rewards/discount/chance, version lineage, public/private leakage gates, NDJSON/MuZero/RLDS exports and round-trip verification. Eligibility stays false until independent approval.

### M8 — Production hardening

- Identity/RBAC, KMS, multi-instance CAS, persistent scheduling, immutable releases, privacy retention, telemetry, cost control, incident rollback and real-device evidence.

### M9 — Handoff and acceptance

- Architecture/ADR index, source and contract manifests, verifier matrix, expert-readable records, screenshots/video/PDF evidence, known-debt ledger and implementation handoff.

## First implementation increment

The first increment is being upgraded from the prototype in-memory slice to an authoritative engine, a durable `RoomStore`/room runtime, and a transport-neutral HTTP Adapter. The deep mutation interface is:

```text
createEnvelope → legalSpace → preview → apply → replay
```

Its implementation owns RFC 8785 canonicalization, SHA-256 identities, Ed25519 permanent signatures, HMAC short-lived seals, proposal identity, complete hybrid LegalSpace, fixed-point arbitrary-path validation, revision/fencing checks, confirmation policy, deterministic rules time/chance, receipt construction, three-ledger projection, journal chaining, dependency freeze, historical rules display and exact replay. Callers may instantiate only a rules-owned parameter domain; they cannot supply role, side, current state, rules version, authority, or a new arbitrary mutation type.

The root legacy `apply-action` route remains untouched but is restricted to an isolated development sandbox. It cannot create Level-3 receipts, enter Level-3 rooms, or produce training evidence. New clients, rooms and Agents target `/starcraft-tmg-level3/api/v1/rooms/*`; this Adapter delegates every state mutation to the deep authority interface. Root legacy integration happens only after Expo/Battle Lab migration and parity evidence define an explicit boundary.

## Exactness and promotion policy

- Unsupported rules fail closed or remain explicitly display-only; source text is not silently converted into executable truth.
- A successful preview, apply, replay, Provider response, DSH run, or export does not grant rule truth, Skill promotion, or training truth.
- Roleplay/worldbooks, machine translation, user memory, strategy Skills and community material cannot overwrite official rules facts.
- Every accepted state change is revision-bound and receipt-bound. Every replay claim must reproduce the same semantic receipt chain.

## Character-Agent increment

The fixed root now contains the contract-first Kerrigan Adjutant slice:

- versioned CharacterPackage, worldbooks, RoleSkillPacks, ConversationProfile, ProviderProfile and GameRoleBinding integrity contracts;
- separate Tutor, Opponent, Commentator and Companion sessions, prompt packs, tools, memory namespaces, histories and traces;
- a direct-Provider transport boundary whose ProviderProfile cannot contain credentials or select DSH;
- session-memory-only BYOK bind/detach with redacted inspection;
- output-channel validation in which only Opponent may emit a decision, and the decision must select an enabled candidate from the current LegalSpace;
- Opponent preview followed by external human confirmation; no model-facing mode owns apply;
- rights, era, rules-authority and training-truth gates that remain explicitly unpassed.

The mature-worldbook follow-up adds eight timeline persona editions plus one TMG context book, a single-persona selection invariant, spoiler and knowledge rank ceilings, deterministic entry budgets and activation receipts. It also adds a Character Card V2 JSON Adapter: sealed Project D cards round-trip by integrity hash, while external prompts are quarantined and imported cards default to untrusted Companion-only mode. PNG embedding remains an explicit gap. A Project D-original `Vesper` fallback now proves the same four role/session contracts, but its visual/voice asset gate remains open.

This is not yet the required M1 live demo. Its verifier uses an injected fake Provider transport, and no Web/App screen, real Provider response, browser trace, or device evidence has been run.

## Direct Provider increment

Ticket 16 now has a concrete OpenAI-compatible direct-Provider Adapter and Harness receipt path:

- server-sealed ProviderProfile contracts validate numeric budgets and forbid embedded credentials, DSH, and internal retry;
- remote Provider endpoints require HTTPS, reject credentials/query/hash in base URLs, and normalize only a bounded configured chat-completions path;
- session BYOK is placed only in the outbound Authorization header and is absent from request bodies, outputs, summaries, traces, and safe receipts;
- the Adapter sends the already assembled prompt/room/LegalSpace evidence and a channel contract, but exposes no model-owned Referee tool or arbitrary tool-call path;
- request/response bytes are bounded, header and body timeouts share one AbortController, error bodies are not retained, and the Adapter makes exactly one physical attempt;
- successful receipts bind ProviderProfile, model, safe usage, request ID, response fingerprint, elapsed time, and zero internal retries; the session verifies receipt integrity, profile identity, and raw-output binding before recording the Harness trace.

This is still injected-fetch evidence only. No billable request has been made. Isolated credential workers, egress allowlists, persistent attempt/WAL accounting, administrator budgets, crash recovery, live model/version evidence, browser consent flow, and production identity/KMS remain open.

## Offline Skill execution-arm increment

Ticket 17 now has a contract-first DSH tracer bullet without installing or running DSH:

- `SkillGenerationJobManifest` seals the source/input/existing-Skill/prompt/runtime/provider/tool/permission/budget/Scheduler identities and keeps all online Room, Rules mutation, publication, and training capabilities false;
- the DSH arm is pinned to audited `@deepseek-ai/dsh 0.1.1-rc.2` / `b150a551...`, exact effective-config and plugin-lock hashes, an OS-isolation profile, provider-only egress, disabled telemetry, and zero internal retry;
- a DSH-off direct-Provider control arm implements the same interface so paired experiments can hold staged input, model, tool schema, Token/cost/wall budgets, and output contract constant;
- the execution Adapter accepts only read-only staged source/existing-Skill tools and exactly one `emit_candidate_skill`; multiple submissions, unstaged citations, credential-bearing inputs/outputs, and budget/retry escape fail closed;
- `CandidateSkillBundle` always carries game/rules scope, source refs, procedure/legality material, Judge tests, unresolved claims, human-review state and promotion blockers while keeping strategy, Rules and training authority false;
- `RunReceipt` binds Scheduler attempt/lease/fence, runtime/config/plugin hashes, execution session and log, Provider attempts, mutually exclusive input/cache Token accounting, output/reasoning usage, price table/cost, candidate hash and credential scan.

The verifier uses injected executor ports only. DSH installation, disposable-container enforcement, a real DSH Session log, Provider calls, schema/fact/Judge/Cross-Time/held-out gates, quality A/B and administrator promotion remain open.

## Source and translation increment

The fixed root now also contains the first M4 contract slice:

- a source registry that records source class, authority class, canonical and prohibited scopes, review state, snapshot requirements, and redistribution status independently;
- immutable raw-snapshot receipts whose SHA-256 and snapshot identity are content-bound without embedding the raw payload;
- a normalized-dataset manifest that binds transformer and input snapshots, record hashes, lineage gaps, executable exactness, redistribution, and training eligibility;
- a read-only legacy-pack Adapter that inventories the current local StarCraft data but keeps product Firestore candidates, community material, Project D-derived presets, and sample content distinguishable;
- glossary-bound machine-translation intents and reviewable display sidecars that reject rule/numeric fields, keep DSH outside translation runtime, never overwrite canonical source data, and fail closed to canonical text after source drift;
- a deep source/localization runtime and transport-neutral HTTP Adapter that keep canonical lookup, Provider invocation, draft visibility, authenticated review, provenance rendering, and failure policy behind one shared Web/App interface.

The legacy pack is not official truth and is no longer a candidate runtime fallback. The current official Command Center `BETA v1.4` snapshot has now been captured from its public named Firestore database as `unitsVersion=71 / cardsVersion=69 / rulesVersion=48`, with `26` unit, `37` tactical-card, `191` faction-card, and `15` rules-section records. Those records remain captured-unreviewed until schema, per-record scope, PDF/P2P precedence, and drift checks pass, but all future current-data normalization must start from that official snapshot rather than codebase values. The provisional zh-CN glossary is explicitly non-official and still requires human review. Runtime verification is drafted against an injected fake Provider Adapter; a real translation provider, identity system, persistent correction store, administrator UI, offline cache, and human-reviewed bilingual corpus remain open.

The official-latest-data contract now selects only the unique maximal Command Center version tuple, rejects repository/legacy candidates and version/content conflicts, requires signed schema/scope/community/P2P review evidence, freezes the exact certified source snapshot and normalized dataset into each new room, and resolves historical rooms only by both hashes. The strict Adapter passes `11/11` and produces dataset `38f89...e63` with 83 official-current product candidates, 171 isolated community records, and 15 isolated rule-prose candidates. The binding verifier passes `12/12`; the live `c737...a61` snapshot deliberately remains `productionRoomBindingEligible=false` until an externally managed production signer independently approves the machine-reviewed evidence bundle.

The P2P alias/precedence gate now passes `11/11`: all 83 current product records bind 123 content-hash/page-kind historical locators with zero missing records or invalid page kinds. Alias index `cedbb9...b1ac` contains no gameplay payload, makes no value-parity claim, and fixes Command Center as the current-value winner. Its schema/scope/community/P2P evidence bundle `e6b23a...de63` replaces synthetic certification hashes. This closes the machine contract, not the independent production signature gate.

Core clause work now has a corrected reproducible candidate denominator: `1,638` sentence candidates across all `192` anchor regions, eleven independently isolated display-only Part 2–12 preambles, four structural-only parent headers, two excluded decorative-glyph-only lines, and zero uncovered non-whitespace characters. The denominator passes `12/12` at hash `64cf70...05aa`; superseded `54b727...80f` and intermediate `27220b...e277` artifacts must fail dependency binding. Parts 2–10 classify all `1,188/1,188` candidates from 107 anchors exactly once into `723` reviewed semantic boundaries (`614 review_required`, `109 display_only`). Part 8 remains closed at full ledger `962bd3...f686b`; Part 9 closes at `82863e...e62e0`; Part 10 closes 69 candidates into 54 boundaries (`52/2`) at `a2e516...5d93e`. Combined coverage index `f0572a...3717` covers Parts 2–10 and retains Parts 11–12 / 450 candidates as explicit gaps. Part 11's 73 glossary anchors / 351 candidates use six source-ordered no-anchor-split batches `51/43/70/71/55/61` at plan `f6ddbe...a9ef`; 11A closes 51 candidates into 44 review-required boundaries at ledger `660b31...5b03`, 11B closes the next 43 candidates into 38 review-required boundaries at ledger `474a24...9c80`, 11C closes the next 70 candidates into 58 review-required boundaries at ledger `2824fb...d01b`, and 11D closes the next 71 candidates into 60 review-required boundaries at ledger `356062...3ef6`. The partial merge now covers 48/73 anchors, 235/351 candidates and 200 semantic boundaries, leaving 11E–F / 116 candidates; it cannot enter global coverage before every remaining batch and the full merge. Missing, duplicate, tampered and dependency-drifted inputs fail closed. All artifacts preserve `executable=false`, `trainingTruth=false`, and global `canonicalClauseCount=null`. The Ticket 11 foundation aggregate passes `329/329`. The rebound FAQ gate passes `10/10` at `25cab2...2a71`. Candidate-level FAQ reconciliation, Part 11E–F, Part 12, RuleAtoms, executors, Judge tests, interactions, lifecycle and cross-version replay remain open.

Superseding Part 11 checkpoint: Part 11E closes 55 candidates into 50 review-required clauses at batch ledger `660af5...3b9e`; Part 11F closes 61 into 50 at `928eb9...5ec7`. The full merge exposed a repeated Effective Size/Ground Level clause identity and now preserves both source candidates under unique source-local IDs; the revised 11B ledger is `b844a4...d8e2`. Part 11 is therefore complete at the semantic-review layer: 73/73 anchors, 351/351 candidates and 300 review-required boundaries, full ledger `f27494...f69b9`. Combined coverage `6bbc7b...55e6` covers Parts 2–11 with 180/192 anchors, 1,539/1,638 candidates and 1,023 local boundaries (`914 review_required`, `109 display_only`); only Part 12 / 99 candidates remains. The aggregate passes `346/346`. Executable/global-canonical/Rules/Skill/training authority remains zero; Part 12, exact FAQ reconciliation, RuleAtoms, executors, Judge/interaction/lifecycle/cross-version replay remain open.

The core source-filtering front end is now complete. Part 12 closes all 12 quick-reference anchors and 99 candidates into 67 local clauses (`62 review_required`, `5 display_only`) at `5a4564...9b99`; PDF product tables remain historical display only and cannot override current Command Center data. Coverage `dc9c33...d7a0` now includes Parts 2–12, all 192 anchors and all 1,638 candidates, producing 1,090 local boundaries (`976 review_required`, `114 display_only`) with no uncovered source part. The aggregate passes `356/356`. This does not create executable rules: global canonical merge remains null and FAQ exact reconciliation, cross-anchor deduplication, RuleAtoms, executors, Judge and replay are still required.

The FAQ semantic front end is also complete without rewriting history. Immutable v1 remains the broad-link receipt; exact v2 maps the three normative FAQ entries to 15 core local clauses and 18 exact candidates at `eb5263...3432f`. The current Command Center deployment records expose format and card-image identity but no battlefield-dimension field, so v3 does not invent a current-data value. Instead it classifies all three previously unmatched FAQ claims at `6d1e52...3b40`: Skirmish 36×36 and terrain-height assignment at game start become two review-required RuleAtom candidates, while metric equivalents remain one display-only reference. The resulting local merge input is 1,093 clauses (`978 review_required`, `115 display_only`) and the aggregate passes `380/380`; execution, Rules, Skill and training authority all remain false pending global canonical merge, RuleAtom mapping, executors, Judge and replay.

Global canonical mapping is now underway. Plan `8068f...4618` partitions all 1,093 local clauses into 19 operational review batches with zero omission or duplicate assignment and flags 14 exact/normalized-label duplicate candidates covering 28 clauses; these hints cannot auto-merge. Batch 1 manually resolves 12 groups as equivalent and keeps Movement/Assault Hold plus general Faction Tag/Faction Card eligibility distinct, yielding 16 canonical clauses at `f881fd...83e2`. Thus 28/1,093 local clauses are reviewed and 1,065 remain; final global canonical count is still null. The aggregate passes `406/406`, and the next pass expands medium-confidence cross-section paraphrase candidates before singleton mapping so wording drift cannot create false unique rules.

The medium-confidence source pass is now evidence-backed rather than title-only. Expansion `083601...b4fd` verifies every one of the 1,090 core local clauses against its review-packet source-text hash, excludes Batch 1, and emits 27 disjoint normalized-character or high-token-equivalence groups covering 54 clauses; it cannot auto-merge. Batch 2 manually merges 21 genuinely equivalent groups and keeps six phase, record-type, entry-mechanism, or activation-scope pairs distinct, producing 33 canonical clauses at `c5eda2...2f5f`. Cumulative mapping is 82/1,093 local clauses into 49 partial canonical clauses with 1,011 remaining. The full 45-report aggregate passes `426/426`; final global count stays null, executable RuleAtoms stay zero, and Rules/Skill/MuZero authority remains blocked pending broader containment review, singleton mapping, executors, Judge and replay.

The containment/title pass is also closed. Expansion `4f3e1e...6e8e` examines the 1,008 remaining core clauses after Batches 1–2 and produces 40 source/title evidence pairs, 31 connected review groups and 69 unique clauses with zero overlap. Human Batch 3 merges only seven full equivalences and keeps 24 same-shape or subset relationships distinct where prerequisites, subjects, phases, entry mechanisms or semantic granularity differ. Its 69 locals become 62 partial canonicals at `1c99fa...7d97`. Cumulative mapping is now 151/1,093 locals into 111 partial canonicals with 942 remaining; the 47-report aggregate passes `445/445`. Before singleton materialization, a residual collision pass must compare remaining locals both with one another and with already-reviewed canonicals, allowing a newly found alias to extend an existing canonical without remapping its prior source. Final global count and all execution/Skill/training authority remain blocked.

The global canonical front end is now complete. Residual expansion `167ab2...2af0` binds all 1,093 core+FAQ clauses, excludes mapped-to-mapped reopening, and combines 85 bounded heuristic pairs with five hash-bound human probes into 90 pairs / 56 connected groups / 137 unique clauses. Human Batch 4 classifies those groups as four pure existing-canonical extensions, twelve new-equivalence merges, thirty-five context-preserving splits and five explicit partitions. Batch `58068f...022f` maps all 111 remaining candidates, adds six aliases to five existing canonicals with zero prior-source-row mutation, and creates 84 new canonicals. The final explicit singleton policy freezes the remaining 831 collision-free locals. Finalization `f9a6aa...3dfd` maps all 1,093 locals exactly once to 1,026 canonical clauses; catalogue `2ca56c...4140` has zero missing, duplicate or unknown mapping. The 50-report aggregate passes `469/469`. This closes source screening and canonical mapping only: all 978 rule-eligible local clauses remain non-executable, and RuleAtom typing, Executor/LegalSpace coverage, Judge interactions/lifecycle and replay are now the active Ticket 11 gate. Canonical completion grants no Rules, Skill or training authority.

The implementation has now crossed from screening into the Rules engine. The official adapter binds the frozen 1,093-clause source denominator to all 1,026 canonical RuleAtoms at `e46363...0f1`, retaining source, FAQ, review-ledger and complete-Part identities; an empty executable set fails closed. The first deliberately bounded executable family is Movement Hold: three typed atoms now flow through Rules-owned LegalSpace, deterministic executor, Preview, signed/sealed Receipt and Replay at slice `c8e1cb...013b`. Hold mutates no board, status, damage, resource or score state; it marks the unit's movement activation and hands off deterministically, while invalid phase, seat, reserve, destroyed and repeat-activation paths reject. Move remains blocked because same-unit passage, terrain access/size, Flying and whole-unit coherency are not yet exact. Current disposition is 3 executable / 909 review-required / 114 display-only with zero non-target atom changes. The 52-report Ticket 11 foundations aggregate passes `485/485`; this grants Rules/replay authority only to Movement Hold and grants no Skill, MuZero or training authority.

The second cumulative Rules slice closes Movement/Assault Passing and phase handoff. Executor `authority.activation-pass-v1@1.0.0` promotes eleven canonical atoms for optional and mandatory Pass, first-passer next-phase priority, phase lockout/opponent completion, Movement/Assault marker completion and phase-specific initiative. Slice `d4d77b...e453` and catalogue `4af110...a378` bring the total to 14 executable / 898 review-required / 114 display-only atoms. Movement/Assault LegalSpace no longer exposes client `advance_phase`; inactive and passed seats receive no activation authority. After the first Pass, the opponent completes consecutive remaining activations, and its final official Hold advances the phase automatically with the relevant Pass/phase lineage in Preview, Receipt and Replay. The availability predicate is shared by LegalSpace and apply and includes reserve deployment, so a player with an available Deploy is not mislabeled mandatory. The 53-report aggregate passes `494/494`; no Skill, DSH, MuZero or training promotion is granted.

The official execution seam is now explicit instead of being mixed with the legacy enumerator. `OfficialExecutableRuleRuntime` validates the exact `4af110...a378` catalogue, its two known executor versions, all source snapshot hashes and the 14-atom resolved set before exposing the deep `descriptor / enumerate / apply` interface. AuthorityEngine binds runtime identity into MatchBinding, frozen rules/executor dependencies, LegalSpace and health; RoomRuntime carries the same identity into room summaries and rejects a requested production room with `PRODUCTION_RULE_RUNTIME_REQUIRED`. Because 1,012 atoms remain non-executable, strict official mode fails LegalSpace with `RULE_RUNTIME_INCOMPLETE`; only an explicit development subset can exercise Hold/Pass, and that subset is visibly non-production and training-ineligible. Legacy enumeration is now identified as `legacy_compatibility_fixture` and cannot silently fill official actions. Runtime verifier `10/10`, Hold `7/7`, Pass `9/9`, and authority/HTTP `15/15` pass. The Ticket 11 foundations aggregate is now 54 reports / `504/504`; RuleAtom count and project completion remain 14/1,026 and 10/22.

The third executable family adds Assault Hold without treating it as Movement Hold. Executor `authority.assault-hold-v1@1.0.0` promotes the distinct Assault marker, no-action and phase-summary atoms; slice `bc7061...fcf`, catalogue `ad80d79...85f`, and runtime `0050ee...781` now bind 17 executable / 895 review-required / 114 display-only atoms. The executor accepts only the active seat's live, on-field, Assault-unactivated Unit, marks only `activatedPhases.assault`, and leaves board, status, damage, resources and score unchanged. Runtime settlement preserves ordinary alternation and advances to Combat only when a prior Pass leaves no eligible activation; implementing this exposed and fixed the shared phase-activation callback's argument order, then replayed Movement Hold and Pass unchanged. Assault Hold passes `8/8`, runtime `10/10`, Movement Hold `7/7`, Pass `9/9`, and authority/HTTP `15/15`; the full foundation sequence passes 55 reports / `512/512`. This remains a non-production development subset and grants no Skill, DSH, MuZero or training promotion.

The fourth executable family corrects phase-start priority instead of preserving an inaccurate default. Official 8.2, 8.8 and glossary evidence says the First Player Marker holder chooses which player activates first; the earlier 14/17-atom subset had attached `general-first-player-phase-priority` to Pass and then defaulted the marker holder itself. Slice `d4f536...6cfd` therefore promotes two additional atoms and version-reassigns that existing atom to `authority.phase-initiative-v1@1.0.0` as atom v2, without mutating historical catalogue `ad80d79...85f` or runtime `0050ee...781`. Current catalogue `e33117...3777` and runtime `f82a35...cb93` expose exactly two `choose_first_actor` actions to the marker holder at each pending Movement, Assault or Combat start; either seat may be selected, and Hold/Pass remain hidden until selection. Movement→Assault and Assault→Combat handoffs reopen the choice window, while Preview, explicit human confirmation, Receipt and Replay retain exact lineage. Current disposition is 19 executable / 893 review-required / 114 display-only; the initiative gate passes `10/10`, latest runtime `10/10`, old slice identities remain exact, and the full foundations gate passes 56 reports / `522/522`. No Skill, DSH, MuZero or training promotion is granted.

The fifth executable family introduces exact ground engagement and Combat Pass as one bounded vertical slice. A live check of the official core download retained SHA-256 `27639c562e6db9777dd9ba984d0c9f9b581841ec30166848e021f893cd00ea54`; no repository rules or unit-data fallback was used. `EngagementGraph` derives model-level edges from frozen room coordinates, round base footprints, ground elevation/combat tags and explicit board/terrain geometry, then propagates engagement to Units. Size 0–1 terrain does not block engagement, size 2+ terrain may block it, and Flying models remain unengaged; missing bases, model-count mismatches, incomplete terrain/elevation declarations, elevated models, access points and non-round geometry fail closed rather than being guessed. Executor `authority.combat-pass-v1@1.0.0` promotes twenty exact atoms and exposes mandatory Pass only when the active seat has no remaining unactivated Engaged Unit. The first Pass hands activation to the opponent and the second advances Combat to Cleanup; Preview, Receipt and Replay bind the engagement-graph hash. Slice `50782a...f60a`, catalogue `88a1fa...5ad`, and runtime `73f209...ba4d` now bind 39 executable / 873 review-required / 114 display-only atoms while historical catalogue/runtime identities remain exact. Combat Pass passes `9/9`, current runtime `10/10`, and the full foundations gate passes 57 reports / `531/531`. Exact elevated/access/non-round geometry, Combat activation and attack resolution remain open, so the subset is non-production and grants no Skill, DSH, MuZero or training promotion.

The sixth executable family completes the official elevation and Access Point denominator needed by engagement without inventing a Combat Hold action. Part 8.8 requires every Engaged Unit to activate and make a Close Combat Attack; Close Ranks is optional before that attack, so the next action slice is Close Combat Attack rather than Hold. `EngagementGraph` v2 derives ground/mid/high support from explicit terrain surfaces, proves support and Access Point declarations against footprint intersection, permits same-elevation engagement, rejects ground/high engagement, and requires a shared exact Access Point for ground/mid or high/mid engagement. Flying still ignores elevation and remains unengaged. Historical v1 catalogue `88a1fa...5ad` and runtime `73f209...ba4d` stay byte-identical; executor `authority.combat-pass-v2@2.0.0` reassigns the prior twenty Combat Pass atoms and promotes fourteen elevation/access atoms. Current slice `bcc48d...5c40`, catalogue `aa307f...7263`, and runtime `0eab90...535` bind 53 executable / 859 review-required / 114 display-only atoms with zero non-target changes or contract/evidence gaps. The elevated slice passes `12/12`, current runtime `10/10`, all historical rule slices and authority/HTTP remain green, and the full foundations gate passes 58 reports / `543/543`. Non-round bases and the Close Combat Attack lifecycle remain open; no Skill, DSH, MuZero or training promotion is granted.

The seventh executable family implements a real but bounded Close Combat Attack against current official Command Center unit profiles. Executor `authority.close-combat-attack-v1@1.0.0` promotes forty-eight atoms for the single-enemy-Unit, single-target-model, no-Close-Ranks, simple-weapon subset with no explicit melee Evade, Surge, upgrade, ability or external modifier. Fighting and Supporting Rank are derived from exact engagement and same-Unit base-contact graphs; preallocated D6 outcomes resolve Hit, Armour, damage markers, casualty removal and the post-combat graph. Preview exposes only proposal/state/counter-bound HMAC commitments, Apply reveals outcomes in an Ed25519 receipt, and replay verifies the signed reveal bundle after HMAC-key rotation. Slice `3830d9...766`, catalogue `13e944...95d`, and runtime `b0ac1c...69a` bind 101 executable / 811 review-required / 114 display-only atoms. Focused verification passes `8/8`, the full foundations gate passes 59 reports / `551/551`, and the prior elevated catalogue/runtime remain exact. No Skill, DSH, MuZero or training promotion is granted.

The eighth executable family makes optional Close Ranks and the mandatory attack one atomic parameterized Fight. Executor `authority.close-combat-attack-v2@2.0.0` owns the `official_close_ranks_single_model_path_v1` LegalSpace parameter domain and supports only one active round-base ground model with complete terrain/access/effect-free geometry. A canonical milli-inch path must be at most 3 inches, remain on the board, sweep collision-free, finish strictly closer to an engaged enemy, preserve all current enemy engagements, create no new enemy-Unit engagement relative to the authority-bound Combat phase-start snapshot, and reject already B2B/pinned movement. The finite decline Fight remains available, while unsupported multi-model placement fails closed without hiding the decline path. Preview still hides dice behind ChanceTickets; Apply and rotated-HMAC replay preserve signed reveal evidence. Historical catalogue `13e944...95d` and runtime `b0ac1c...69a` remain exact. Current slice `1fc3ae...a9f`, catalogue `2e456b...adf`, and runtime `006291...9de` bind 116 executable / 796 review-required / 114 display-only atoms. Focused verification passes `10/10` and the full foundations gate passes 60 reports / `561/561`. Multi-model reposition/contact priority, terrain/gap/access/effect traversal, multiple enemies and broader attack modifiers remain open; no Skill, DSH, MuZero or training promotion is granted.

The ninth executable family adds ordered multi-model Close Ranks placement rather than accepting a caller's claim that contact was maximized. Executor `authority.close-combat-attack-v3@3.0.0` owns `official_close_ranks_multi_model_formation_v1`, derives eligible Leading and phase-start pinned models, permits the Leading path through its own Unit, and requires an exact placement denominator for every remaining unpinned model. At each official one-at-a-time placement step the referee enumerates every quantized milli-inch round-base contact point: if any legal Enemy B2B endpoint exists, a non-Enemy-contact placement fails; only after Enemy contact is physically impossible may the model use a legal Friendly Fighting-Rank B2B endpoint. Board containment, overlap, Wholly Within 3-inch coherency and phase-start/current engagement remain authority checks. A seven-model Marine fixture fills six total Enemy contact positions and then proves the final Friendly fallback; pinned models remain fixed and cannot lead. The exact scope remains two live Units, one target model, Ground round bases, no terrain/access/effects and final In Coherency. Historical catalogue `2e456b...adf` and runtime `006291...9de` remain exact. Current slice `5d7b00...f12`, catalogue `318d15...ba0`, and runtime `4c55f4...0cb` bind 123 executable / 789 review-required / 114 display-only atoms. Focused verification passes `10/10`, Authority passes `15/15`, and the full foundations gate passes 61 reports / `571/571`. Terrain/gap/access/grass/displacement, Out-of-Coherency closest placement/casualty, multiple enemies and broader combat modifiers remain open; no Skill, DSH, MuZero or training promotion is granted.

The tenth executable family implements the bounded exact Out-of-Coherency branch of Close Ranks. Executor `authority.close-combat-attack-v4@4.0.0` owns `official_close_ranks_coherency_fallback_formation_v1`; its geometry kernel derives same-Unit Link graphs and validates each follower in official priority order: Enemy B2B, Friendly Fighting-Rank B2B, any legal linked point Wholly Within 3 inches, the closest legal linked point when the first three are unavailable, then immediate casualty only if no Link exists. Ordinary within-three proposals use a complete positive witness. Any “none exists” or “closest” conclusion enumerates the complete integer milli-inch center lattice, with a hard 2,000,000-point ceiling; exceeding that ceiling fails closed and therefore does not prove arbitrary standard-table fallback. The accepted narrow-board fixture produces Enemy contact, Friendly contact, within-three fallback, closest linked Out-of-Coherency, and a no-Link casualty in one ordered Marine formation. Apply atomically records durable coherency status, changes current models from seven to six, recalculates frozen-profile Supply from two to one, and resolves the attack. Preview hides chance results, Ed25519 receipts reveal them, and replay remains valid after HMAC rotation. Historical v3 runtime `4c55f4...0cb` is exact. Current slice `9d5f67...414`, catalogue `db2e3d...e70`, and runtime `a52684...d5f` bind 128 executable / 784 review-required / 114 display-only atoms. Focused verification passes `12/12`; the full foundation gate passes 62 reports / `583/583`. Terrain/access/flying/pinned/multiple-enemy variants, mission-marker Out-of-Coherency enforcement and device UI evidence remain open; no Skill, DSH, MuZero or training promotion is granted.

The eleventh executable family connects that durable coherency result to the first real Cleanup operation: Mission Marker control. Official Core 4.4, 6.2, 8.9.1 and Glossary evidence defines a First-Player-initiated per-marker test. `authority.mission-marker-control-v1@1.0.0` validates the frozen current Official Combat Profile bundle, exact 32mm marker state, battlefield presence, at least one model Within 3 inches, terrain-free line of sight, equal Ground elevation, In Coherency and the Flying/Burrowed/Reserve prohibitions. It then sums eligible Current Supply and applies higher-total control, tied no-transfer, no-contest sticky control and sole Supply-0 control. Apply atomically writes control/faction indicators and advances the explicit Cleanup progress to VP scoring. The public finite action carries per-marker/per-Unit eligibility and Supply explanations, requires explicit human confirmation, and lets an Opponent model preview but never self-confirm or apply. The exact scope is round bases on Ground with complete marker declarations and no active terrain; terrain LoS/elevated support fail closed, as do duplicate model identities, missing active-model declarations and bases outside the battlefield. Historical v4 catalogue `db2e3d...e70` and runtime `a52684...d5f` remain exact. Current slice `2d1b8e...d102e`, catalogue `9a841f...c0217`, and runtime `df4488...e2ef1d` bind 150 executable / 762 review-required / 114 display-only atoms. Focused verification passes `15/15`, signed replay survives HMAC rotation and rejects tampering, and the full foundation gate passes 63 reports / `598/598`. VP scoring, game-end resolution, scoring/cleanup effects, initiative, terrain LoS/elevated support and device UI remain open; no Skill, DSH, MuZero or training promotion is granted.

The subsequent compatibility/evidence hardening increment adds no RuleAtom and is not a twelfth rules slice. The retained Transition, Room, HTTP, Kerrigan, direct-Provider and fallback-worldbook verifier commands now exercise the current Authority v2 contract: server-owned initial state, server-issued SeatGrant, Rules-owned proposals, human ConfirmationReceipt, fenced ControlLease, Ed25519 AcceptedReceipt and replay. `CharacterSessionFactory` can resolve a role's SeatGrant through a server-only resolver; client summaries expose only `seatCredentialSource`, never bearer material, and client-supplied seat credentials remain rejected. The generic executable-runtime gate was also advanced from the stale 53-atom elevated catalogue to the current 150-atom Mission Marker catalogue. A new eight-item cross-report aggregate verifier first exposed that drift, then machine-counted 62 base reports / `590/590` plus its own report as the documented 63 reports / `598/598`; it independently binds the current hashes, historical v4 hashes and old-rules display, official `71/69/48` data lineage, seven-executor manifest, production fail-closed state and zero Skill/training promotion. `verify:all`, Authority `15/15`, syntax and secret-sentinel checks pass. These are fixture/injected-transport results only: no live Provider or DSH execution, browser/device UI, PostgreSQL production connection, Skill promotion or training candidate was produced.

The twelfth executable family implements one deliberately bounded VP-scoring step rather than claiming the complete Phase 4 lifecycle. The source review in `docs/research/official-vp-scoring-source-review-2026-08-25.md` freezes Core PDF `27639c...ea54`, current Command Center `71/69/48`, all ten current official mission records, and exact Hold Position Standard provenance; repository rules or card data remain forbidden fallbacks. `OfficialGameplayDataBundleV1` composes the existing combat profiles with exact mission scoring data, while `OfficialMissionSetupBindingV1` binds server-owned draft receipt hashes, one Red/one Blue seat assignment, affinities `1/3 Red`, `2/4 Blue`, `5 Neutral`, and the invariant that affinity never grants control. A runtime-hash-bound append-only Supply-loss ledger now records every supported Supply decrease with causal lineage. `authority.close-combat-attack-v5@5.0.0` writes that ledger, and an Out-of-Coherency casualty is explicitly recorded as `unresolved_official_source / scoreable=false`; `authority.mission-marker-control-v2@2.0.0` consumes the same composite binding and ledger without rewriting historical v1/v4 behavior. `authority.victory-point-scoring-v1@1.0.0` then supports only Hold Position Standard in rounds 2–4 with a proved zero-delta round ledger: it derives both players' marker VP from one before-state, commits both `ScoringBreakdown`s and totals atomically, and advances to `check_end_game_conditions`. Any nonzero or tampered ledger, missing draft provenance, source/runtime drift, wrong step/round/seat, or model-side Apply fails closed; Opponent preview remains non-mutating. Twelve atoms are newly executable and 97 prior Fight/Marker atoms are explicitly version-reassigned. Current slice `5a0e91...8cc2`, catalogue `f831ba...df38`, and runtime `61a688...4039` bind 162 executable / 750 review-required / 114 display-only atoms; historical Mission Marker catalogue/runtime `9a841f...0217` / `df4488...ef1d` and v4 `db2e3d...e70` / `a52684...d5f` remain exact. Focused verification passes `15/15`, the full foundation gate passes 64 reports / `613/613`, Authority passes `15/15`, and `verify:all` passes. Special/end-game conditions, nonzero Supply attribution, End-of-Round effects, object/card Cleanup, Initiative/Roll-Off, final scoring, other missions, browser/device evidence, production PostgreSQL and production data signing remain open; no Skill, DSH, MuZero or training promotion is granted.

The thirteenth executable family closes only the unambiguous Hold Position Standard special-win check after that frozen scoring prefix. `authority.hold-position-end-game-check-v1@1.0.0` promotes the Core end-game-check step and 8.9.3 special-win terminal atoms. It accepts rounds 2–4 only when marker control and VP scoring are the exact current-round history tail, current scores match the signed scoring result, the current official gameplay bundle/MatchBinding still agrees, and both players expose at least one complete live battlefield-model witness. A lead of at least 10 writes one terminal winner/reason/history, and the runtime then exposes no legal action plus one exact terminal summary; a smaller lead advances only to `resolve_end_of_round_effects`. This witness requirement excludes an unresolved simultaneous Army Elimination trigger. Army Elimination itself, the surviving-player +10 award, Round 5/final scoring, final reserve destruction, tiebreak/draw and multiple-terminal-trigger priority are not inferred and fail closed. Slice `fa488e...f51a`, catalogue `b6d22d...9c3c`, and runtime `58fac9...5b71` bind 164 executable / 748 review-required / 114 display-only atoms across 13 vertical slices while historical scoring `5a0e91...8cc2` / `f831ba...df38` / `61a688...4039`, Marker and v4 hashes remain exact. Focused verification passes `15/15`, current runtime `10/10`, historical gates `15/15 + 15/15 + 12/12`, Authority `15/15`, `verify:all`, and the full foundation gate passes 65 reports / `628/628`. No Skill, DSH, MuZero, memory or training promotion is granted.

The fourteenth executable family closes only a proved-empty End-of-Round effect window after that exact non-terminal Hold Position prefix. `authority.end-of-round-effects-v1@1.0.0` promotes the window and empty-resolution step atoms, but deliberately leaves the non-empty First Player/player-selected ordering atom at `review_required`. The executor accepts Standard rounds 2–4 only when marker control, VP scoring and the no-special-win check are the exact current-round history tail; the current official Marine and Hold Position records are hash-bound; selected upgrades, statuses, combat effects, card resources and board effect markers are all absent; and the derived complete effect queue is empty. Apply records the proof hash and advances only to `cleanup_and_refresh`, without mutating scores, pieces, board or card resources. Unknown or non-empty sources fail `END_OF_ROUND_EFFECTS_SOURCE_SCOPE_UNRESOLVED`; progress/order drift fails closed. Opponent models may Preview but cannot Apply, and the four-step marker → score → end-game → effect-window replay survives HMAC rotation while Ed25519 tampering fails. Slice `9e5609...d78de`, catalogue `0a697b...2e9c`, and runtime `a331ac...85f` bind 166 executable / 746 review-required / 114 display-only atoms across 14 vertical slices. Focused verification passes `11/11`, current and historical runtime gates remain exact, Authority and `verify:all` pass, and the full foundation gate passes 66 reports / `639/639`. Army Elimination remains unimplemented because current official text does not settle whether the surviving-player `+10 VP` is itself a direct win or how simultaneous terminal triggers are prioritized. No Skill, DSH, MuZero, memory or training promotion is granted.

The fifteenth executable family implements the exact supported Cleanup/Refresh step after that proved-empty effect queue. Current official Academy and Terran Armed Forces records are source-hash bound; EOR v2 explicitly reassigns the prior two atoms so those card instances enter the empty-queue proof, while `authority.cleanup-refresh-v1@1.0.0` promotes four Cleanup atoms. It resets Unit phase activations and per-round Pass state, refreshes only supported exhausted cards, preserves board/damage/score/history, and advances only to `determine_initiative`. Unknown sources, readiness mismatch, active effects/statuses or unresolved generic markers fail closed. Slice `4a77ae...76a0`, catalogue `99c331...e4be`, runtime `3bed86...71ef4` bind 170 executable / 742 review-required / 114 display-only atoms across 15 vertical slices; focused `12/12` and historical replay remain green.

The sixteenth executable family implements post-Cleanup Determine Initiative for Hold Position rounds 2–4. `authority.determine-initiative-v1@1.0.0` promotes six atoms: a lower-VP seat deterministically receives the next First Player Marker, while tied VP creates four hidden D6 `ChanceTicket`s (2D6 per seat), records the reveal under the Ed25519 AcceptedReceipt, and requires a new committed attempt after every tied total. The winner advances the round, resets the new round's Supply-loss/scoring ledgers, and stops at `start_of_round` with no active seat. This is deliberately not a Movement handoff: the current mission grants two Supply per round and the Start-of-Round window remains a separate required slice, so no activation is exposed until that window is executable. Wrong seat/phase/progress/score, terminal state, stale action or invalid Chance reveal fails closed; Opponent can Preview but cannot Confirm/Apply. Slice `366e4c...a4483`, catalogue `dfa0f1...11267`, runtime `7edeb5...d558` bind 176 executable / 736 review-required / 114 display-only atoms across 16 vertical slices. Focused `11/11`, runtime `10/10`, Authority `15/15`, application regression and the full 68-report / `662/662` foundation gate pass. Live 2026-08-25 revalidation keeps Core PDF `27639c...ea54`, versions `71/69/48` and all 83 official product payloads unchanged; two community-display-only missions changed only their upvote metadata, proving the remaining need to separate gameplay projection identity from hot community-display identity. No Skill, DSH, MuZero, memory or training promotion is granted.

The seventeenth executable family resolves the next exact Start-of-Round window instead of silently skipping to Movement. `authority.start-of-round-v1@1.0.0` promotes thirteen supply, availability, Reserve, final-round, effect-order, Stationary and Ready atoms for Hold Position rounds 2–5 with only the current Marine, Academy and Terran Armed Forces records. It derives finite pools `6 + 2 × (round-1)` through Round 4 and explicit unlimited Supply in Round 5; Current Supply is recomputed from the official Marine squad profile, Reserves are excluded from the on-table cap but retained in the receipt, and an over-cap finite state rejects. Every live field or Reserve Unit gains Stationary, supported cards end Ready/face-up, and a complete First-Player-then-Opponent effect queue plus round-supply hash is committed before Movement opens its two legal `choose_first_actor` choices. Unknown or Burrowed effects, unsupported data, squad-Supply mismatch, stale resolution, repeated phase choice and dependency drift fail closed. Slice `8ba3a8...f9b5d`, catalogue `2fbc56...d8fd4`, runtime `454d0a...14be0` bind 189 executable / 723 review-required / 114 display-only atoms across 17 frozen slices. Focused `13/13`, runtime `10/10`, Slice16 `11/11`, Authority `15/15`, application regression and the full 69-report / `675/675` foundation gate pass. The remaining slice count stays unfrozen because a vertical slice may promote one or many atoms; 723 is the remaining actionable-atom count, not 723 slices. Round 1, Burrowed/Hidden and nonempty bespoke Start-of-Round triggers remain open, and no Skill, DSH, MuZero, memory or training promotion is granted.

The eighteenth executable family implements the first exact Reserve Deploy operation. Source review freezes current Command Center `71/69/48`, GAUNTLET record/image SHA-256 `1ac74d...fcb`, Marine `4/7` Speed/Supply and latest Terran P2P May 2026 page-1 `32mm` base; repository fallback is forbidden. `authority.reserve-deploy-v1@1.0.0` exposes a continuous full-Entry-Edge coordinate, Leading Model path and ordered remaining-model placements for the GAUNTLET Standard / Marine / 32mm round Ground / no-terrain subset. Rules verify current round Supply and Available Supply, Speed, whole-base board containment, swept collision, final overlap, enemy one-inch engagement, opponent six-inch Zone of Influence and direct unblocked coherency. Apply atomically enters all models, removes Stationary, marks Movement activated, recomputes the supply ledger and hands alternating activation to the other seat; the phase-first actor remains fixed and is not confused with the current active seat. Unsupported unit/deployment/base/terrain/access/token/upgrade/special-deploy inputs, stale domains and source or geometry drift fail closed. Slice `8d0b4d...fdd4`, catalogue `821103...df5e`, runtime `4b28cf...47a6` bind 219 executable / 693 review-required / 114 display-only atoms across 18 frozen slices. Focused `13/13`, runtime `10/10` and the full 70-report / `688/688` foundation gate pass. Exact remaining work is 693 actionable atoms; the rolling forecast is about 57 further slices from current 219/18 throughput and is explicitly not an authority denominator. No Skill, DSH, MuZero, memory or training promotion is granted.

The nineteenth executable family implements the first exact on-table Standard Move operation. `authority.standard-move-v1@1.0.0` reuses the frozen current GAUNTLET / Marine / 32mm official binding and exposes one path-and-formation domain per selectable Leading Model for a live, unactivated, Unengaged Ground Unit. The canonical actual path is capped by Marine split Speed, may cross own-Unit starting bases, cannot cross any other Unit, and must keep the whole Leading base on the battlefield. Remaining models are set in supplied order rather than moved; final board containment, overlap, enemy one-inch endpoint exclusion, Wholly Within 3 inches and same-Unit Coherency Link chains are Rules checks. Apply removes Stationary, marks Movement activated, proves the RoundSupplyState hash unchanged and hands activation to the other seat. Terrain/Grass/gap/ramp/access/elevation/Flying, other units/bases and special/forced movement fail closed. Slice `bf5bfc...b123`, catalogue `66580f...a4d5`, runtime `a8f0fa...eb80` bind 229 executable / 683 review-required / 114 display-only atoms across 19 frozen slices while Slice18 stays exact. Focused `12/12`, runtime `10/10` and the 71-report / `700/700` aggregate gate pass. Exact remaining work is 683 actionable atoms; 229/19 throughput still forecasts about 57 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twentieth executable family implements successful Disengage and declaration-time Tactical Mass without claiming the two casualty branches. `authority.disengage-v1@1.0.0` reuses the current GAUNTLET / Marine / 32mm Ground binding and exposes a path-and-formation domain only for a live, unactivated Engaged Unit. It follows standard-move path, blocking, battlefield and ordered-placement rules, then requires every final model strictly outside all Enemy Engagement Ranges with valid Coherency. The declaration freezes every distinct engaged Enemy Unit and Current Supply; own Supply strictly greater than the combined Enemy Supply records Tactical Mass and removes the following Assault Ranged Attack/Charge penalty, while all other successes persist both prohibitions as a hash-bound restriction state. Apply removes Stationary, marks Movement activated, preserves RoundSupplyState and settles alternation. Ordinary-model casualty and Leading-Model failure proposals fail closed and their two atoms remain pending. Slice `b323a7...e67f`, catalogue `908bba...bb3b`, runtime `92b5d5...dbe5` bind 237 executable / 675 review-required / 114 display-only atoms across 20 frozen slices while Slice19 remains exact. Focused `12/12`, runtime `10/10`, aggregate `8/8` and the full foundation gate pass with 71 base reports / `704/704`, plus aggregate for 72 reports / `712/712`. Exact remaining work is 675 actionable atoms; 237/20 throughput still forecasts about 57 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-first executable family replaces only the current Disengage executor with explicit casualty outcomes while freezing v1 for historical replay and old-rules display. `authority.disengage-v2@2.0.0` promotes the ordinary-model-removal and Leading-Model-failure atoms and version-reassigns the prior eight Disengage atoms. Within the GAUNTLET / Marine / 32mm Ground / no-terrain fixed-point subset, a private feasibility proof allows an ordinary casualty only when no legal endpoint exists within three inches of the moved leader; a Leading failure requires no legal endpoint even in a conservative Speed-disk superset. Thus clients cannot choose convenient casualties: unnecessary and unproved removals reject. Ordinary survivors keep their accepted placements; Leading failure removes the leader, leaves other models at their exact prior positions, does not move the Unit and ends Movement activation. Declaration-time Tactical Mass uses pre-casualty Supply, while removal immediately updates Current Supply and RoundSupplyState. The Supply-loss ledger records `disengage_removal` as unresolved and `scoreable=false`, so no opponent VP is invented and no tier change creates no false loss. Slice `88ff14...5f86`, catalogue `d73e76...efe6f`, runtime `e94bd5...4541` bind 239 executable / 673 review-required / 114 display-only atoms across 21 frozen slices while Slice20 remains exact. Focused `13/13`, runtime `10/10`, aggregate `8/8` and the full foundation gate pass with 72 base reports / `717/717`, plus aggregate for 73 reports / `725/725`. Exact remaining work is 673 actionable atoms; 239/21 throughput forecasts about 60 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-second executable family adds the first official-current ranged attack and closes the following-Assault restriction consumer lifecycle. Live revalidation keeps Core PDF `27639c...ea54`, Command Center `71/69/48`, 26 official Army Units and the Marine gameplay projection unchanged. `authority.ranged-attack-v1@1.0.0` binds C-14 rifle Range 12 / Target All / RoA 2 / Hit 3+ / Dmg 1 / Surge Light(D3) and promotes 39 atoms for one unmodified Ground Marine attacking one unmodified Ground Marine on 1.26-inch round bases with no terrain/access/elevation/shields/effects/upgrades. EngagementGraph v2, no-terrain LoS and base-gap range are Rules-derived. Preview commits Hit×2, Armour×2, Evade×0 and Surge×1 hidden D6 tickets; Apply uses D3=`ceil(D6/2)`, caps Light bypass by actual hits, rolls Armour 5+ for the remainder and atomically applies HP-2 damage/casualty. A non-Tactical-Mass Disengage restriction removes Ranged Attack from LegalSpace; Tactical Mass permits it, and either attack or `authority.assault-hold-v2@2.0.0` consumes/archives the restriction so it cannot leak. Historical Hold v1 and Slice21 identities stay exact. Slice `08d813...1be7`, catalogue `735f43...134b`, runtime `01ed2a...b79` bind 278 executable / 634 review-required / 114 display-only atoms across 22 frozen slices, including three Hold atom version reassignments. Focused `13/13`, runtime `10/10`, aggregate `8/8` and the complete foundation gate pass with 73 base reports / 730 assertions plus aggregate = 74 reports / 738 assertions. Ed25519 replay survives HMAC rotation and rejects tampering. Exact remaining work is 634 actionable atoms; 278/22 rolling throughput forecasts about 51 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-third executable family atomizes official attack profiles and effects before expanding weapon coverage. The current `71/69/48` source snapshot compiles all 26 official Army Units into 51 immutable Profiles (23 Assault / 28 Combat), separating base characteristics from thirteen stable parameterized EffectAtom contracts: twelve direct keyword kinds plus Surge. Unknown syntax quarantines the new catalogue, while recognized effects with no handler stay data-visible but rules-ineligible. The first staged kernel implements only Surge armour bypass and LONG RANGE across declaration, hit, effects, evade, armour and damage stages. `authority.ranged-attack-v2@2.0.0` preserves C-14 behavior and adds the official Goliath Autocannon / 80mm base: its normal 12-inch profile hits on 4+, the extended band through 18 inches hits on 5+, and farther targets reject. Preview dynamically commits nine Hit plus nine Armour D6 tickets. Four LONG RANGE/weapon-keyword atoms are promoted and the previous 39 ranged atoms are version-reassigned without mutating ranged v1. Slice `28089a...06a8`, catalogue `40de23...e5a`, runtime `dd3f3b...f5f`, profile catalogue `ee234f...35a` and kernel `beb764...066` bind 282 executable / 630 review-required / 114 display-only atoms across 23 frozen slices. Profile/kernel `12/12`, Slice23 Authority/cross-seal replay `10/10`, runtime `10/10` and aggregate `8/8` pass with 75 base reports / 752 assertions plus aggregate = 76 / 760. The eleven known unimplemented effects, profile modifiers/upgrades, mixed-range batches, Sidearm targeting, terrain, Evade, shields and broader units stay fail-closed. Exact remaining work is 630 actionable atoms; the 282/23 rolling forecast is about 52 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-fourth executable family implements the first independently maintainable tag-conditional damage keyword. `PIERCE [Tag] X` is compiled and validated as one parameterized EffectAtom; on a matching target tag it replaces the Profile Damage characteristic for each damage die, while nonmatching targets retain base damage. Attack kernel v2 uses the official declaration → hit → effects → armour → evade → damage order and freezes kernel v1 unchanged. `authority.ranged-attack-v3@3.0.0` preserves both earlier ranged subsets and adds the exact official Marauder Quad K12 / 50mm versus Armoured Roach / 50mm no-terrain example: two damage dice at Pierce Damage 2 cause four damage and destroy one Roach. Source/profile/base drift, range beyond 12 inches, selected upgrades, invalid Pierce parameters and every handlerless effect reject before authority. One new Pierce atom is promoted and the prior 43 ranged atoms are version-reassigned without mutating ranged v2. Slice `0d6845...a29`, catalogue `72fc25...669`, kernel `63a686...fe1` and runtime `bdb882...19a` bind 283 executable / 629 review-required / 114 display-only atoms across 24 frozen slices; ranged v2 runtime `dd3f3b...f5f` remains exact. Pierce kernel `8/8`, Slice24 Authority/replay `8/8`, runtime `10/10` and aggregate `8/8` pass with 77 base reports / 768 assertions plus aggregate = 78 / 776. Three of thirteen recognized attack effects are now executable; ten known effects, profile modifiers/upgrades, terrain, Evade, shields and broader attack shapes stay fail-closed. Exact remaining work is 629 actionable atoms; the 283/24 rolling forecast is about 54 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-fifth executable family makes Evade and `ANTI-EVADE X` independently maintainable instead of embedding either in an Adept branch. Kernel v3 registers Anti-Evade as the fourth executable EffectAtom and keeps the official sequence declaration → hit → effects → armour → evade → damage. It changes the Evade target number before rolling, never the die result, clamps modified target numbers to 2+–6+, preserves natural 1 failure and natural 6 success, rejects a null Evade characteristic, and preallocates every Chance roll in Preview. `authority.ranged-attack-v4@4.0.0` adds the exact unmodified single-model Adept Glaive Cannon / 40mm against an exactly engaged unmodified Marine / 32mm no-terrain, no-shield example. Because the attacker is engaged, only the exact engagement-edge target is legal; that ranged damage grants Evade, Anti-Evade 1 changes Marine Evade 5+ to 6+, Surge may bypass Armour but not Evade, and only the post-Evade pool becomes damage. Sixteen canonical RuleAtoms cover the effect, modifier direction/timing/bounds/natural results, engaged targeting and Evade eligibility/order/result; the prior 44 ranged atoms are version-reassigned while v3 remains frozen. Slice `3130b7...2fd8`, catalogue `3a558f...fd77`, kernel `6dbe04...1749` and runtime `99510a...7683` bind 299 executable / 613 review-required / 114 display-only atoms across 25 frozen slices. Historical v3 runtime remains `bdb882...19a` and historical rules display remains required. The live Command Center tuple remains `71/69/48`; two community-mission upvotes are admitted only through a hash-bound same-version display-only drift receipt, with zero official-product and zero rule-prose change. The new snapshot `243ecb...571c` and dataset `225c46...2021` remain captured-unreviewed and production-room-ineligible; repository fallback remains forbidden. Community drift `9/9`, Anti-Evade kernel `9/9`, Slice25 Authority/cross-seal replay `6/6`, runtime `10/10`, and the complete foundation gate pass with 80 base reports / 792 assertions plus aggregate = 81 / 800. Four of thirteen attack effects are executable and nine remain fail-closed. Exact remaining work is 613 actionable atoms; 299/25 throughput forecasts about 52 further planning slices. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-sixth executable family separates official weapon replacement from the parameterized `BURST FIRE X/Y` attack effect. Raynor's `C-14 rifle` is not exposed as an unconditional profile: an exact selected-upgrade receipt first removes its linked `Commando Rifle` and installs the replacement for the whole Unit under four Core/Part 9 atoms. Kernel v4 then registers Burst Fire as the fifth independent EffectAtom handler and derives effective Rate of Attack 9 at no more than 8 inches or 6 beyond 8 through Range 12, without changing the immutable profile. `authority.ranged-attack-v5@5.0.0` adds only one Supply-1 Raynor / 40 mm model with exactly that upgrade selected against one unmodified Supply-0 Marine / 32 mm model, unengaged and free of terrain, elevation, access, shield or extra modifiers. Preview commits all 19 close-band or 13 outer-band D6 tickets before Apply. Five atoms are newly executable and the prior 60 ranged/loadout atoms are explicitly version-reassigned; ranged v4 remains exact for historical replay and old-rules display. Slice `44cdf6...74a0b`, catalogue `e6d616...9cb22`, kernel `820d1b...fc8afe` and runtime `36aa2c...bd388` bind 304 executable / 608 review-required / 114 display-only atoms across 26 frozen slices. Burst Fire `8/8`, replacement/loadout `6/6`, Slice26 Authority/cross-seal replay `7/7`, runtime `10/10`, aggregate `8/8` and the complete foundation gate pass with 83 base reports / 813 assertions plus aggregate = 84 / 821. Five of thirteen attack effects are executable and eight remain fail-closed. Exact remaining work is 608 actionable atoms; Parts 11/8/9 own 374 (61.5%) of them, and 304/26 throughput forecasts about 52 further planning slices. The exact distribution and boundaries are frozen in `docs/ticket-11-slice-26-burst-fire-weapon-replacement-2026-08-25.md`. No Skill, DSH, MuZero, memory or training promotion is granted.

The twenty-seventh executable family promotes parameterless `Bulky` before Chance allocation. Kernel v5 consumes exact EngagementGraph evidence and `authority.ranged-attack-v6@6.0.0` proves Raynor's current default Commando Rifle only in the empty-upgrade, one-model Raynor/40 mm versus Marine/32 mm no-terrain subset; the selected C-14 path remains an unchanged v5 delegate. Slice `56589a...3ef55`, catalogue `21927d...89b3a`, kernel `31a733...ab058` and runtime `17c918...12562` bind 305 executable / 607 review-required / 114 display-only atoms across 27 frozen slices. Six of thirteen attack effects are executable. The complete foundation gate is 85 base reports / 828 assertions plus aggregate = 86 / 836. Exact scope and exclusions are frozen in `docs/ticket-11-slice-27-bulky-default-commando-2026-08-26.md`.

The twenty-eighth executable family corrects and executes `CRITICAL HIT X` without rewriting history. Profile catalogue v1 remains frozen with its unused legacy `additionalHits` field; catalogue v2 binds every previous profile hash and explicitly represents the Core rule as `bypassArmourDice`. The independent kernel moves at most X existing Armour Pool dice directly to Damage during Resolve Surge and generates no additional hits. `authority.close-combat-attack-v6@6.0.0` proves current Kerrigan Blades only for one Supply-1 Kerrigan/40 mm against one unmodified Supply-0 Marine/32 mm with one engagement edge and no terrain, shield, status or additional effect. Preview commits six Hit plus six preallocated Armour D6. The proof's two hits both bypass Armour, deal four damage and destroy Marine without inventing a Supply loss; any Dodge evidence fails closed pending the separate Dodge atom. Slice `bac947...515b`, catalogue `64a9b9...de6c`, profile catalogue `f93e70...99b4`, kernel `07e851...304e` and runtime `ee255e...5eb6` bind 306 executable / 606 review-required / 114 display-only atoms across 28 frozen slices. Critical Hit `7/7`, Slice28 Authority/replay `6/6`, runtime `10/10`, aggregate `8/8`, the complete foundation gate at 87 base reports / 841 assertions plus aggregate = 88 / 849, and repository `verify:all` all pass. Seven of thirteen attack effects are executable and six remain fail-closed. Exact remaining work is 606 actionable atoms; 306/28 throughput forecasts about 56 further planning slices. Detailed source drift, hashes and exclusions are frozen in `docs/ticket-11-slice-28-critical-hit-kerrigan-blades-2026-08-26.md`. No Skill, DSH, MuZero, memory or training promotion is granted, and the existing Harness wiring remains a compatibility seam rather than a complete Harness claim.

The prior checkpoint was Slice 41. Slice 40 remains frozen on the Stimpack activation/Non-Lethal/Precision chain. Slice 41 promotes the Part 11 numeric Buff-value atom and consumes current one-model Marine Speed7 + Stimpack Speed3 in a rules-owned maximum 10-inch Move parameter domain. One Authority transition applies exact 1-CP payment, Non-Lethal Damage2 and typed status before movement, then enforces battlefield, base collision and enemy-engagement endpoint constraints. `hybrid_legal_space_v11` retains the ability, payment, status and path-domain identities and requires explicit human confirmation for the resource-bearing Move. Stimpack-equipped base Move without ability use, post-Move use, Run/Charge/Deploy/Close Ranks and other Speed consumers, multi-model/terrain/Flying geometry and Close Combat Precision remain review-required. Slice40 v10 and all earlier contracts and old-rules display remain exact. Slice `e02af291...ab5e7`, catalogue `d3edd16d...712`, runtime `6206a305...ce86`, characteristic kernel `113a7e60...fd5d` and unchanged effect denominator `2bffb9...188b` bind 421/912 actionable atoms (`46.2%`), leaving 491 plus 114 display-only across 41 frozen slices. Focused `16/16`, runtime `10/10`, historical Slice40 `16/16`, foundations 103 base reports / 1,034 assertions plus aggregate = 104 / 1,042, and `verify:all` pass. Exact source, contracts, Harness fields and exclusions are frozen in `docs/ticket-11-slice-41-stimpack-speed-move-2026-08-26.md`. Planning forecasts about 48 further slices. Atom completion remains distinct from composition/LegalSpace/replay/version closure, and full Harness delivery remains broader than rules wiring. No Skill, DSH, MuZero, memory or training promotion is granted.

Slice 43 remains frozen while the current checkpoint advances to graph-selected Slice 44. The graph exposed that frozen Stimpack active v1 could not preserve the Bayonet replacement and frozen Close Combat v8 did not consume the typed Precision grant. `authority.marine-stimpack-active-v2@2.0.0` adds explicit version/loadout delegation, while `authority.stimpack-close-combat-consumer-v1@1.0.0` owns ordinary Strike, ordinary Bayonet, Stimpack Strike and Stimpack Bayonet as four disjoint paths. A separate Close Combat Precision kernel precommits Chance and enumerates the empty set plus every subset of failed Hit dice up to three; ordinary branches never receive the choice. Cleanup and status/marker/history/loadout drift invalidate the domain, and unknown material fails closed. Only Slice44 uses action schema v13; historical rules/runtime identities remain exact. The graph is still derived audit evidence: it expands to `b2415d3c...77a8`, 5,087 nodes / 19,667 edges and 37 executors, with 3 declared state contracts and the same 34-contract debt. Slice `e722cc2e...9a3a6`, catalogue `732fad40...a649` and runtime `7cdcaa4c...acc` remain at 421/912 actionable atoms, 491 remaining and 114 display-only across 44 frozen slices. Focused `15/15`, runtime `10/10`, foundations 106 reports / 1,084 assertions plus aggregate = 107 / 1,093 pass; exact evidence is frozen in `docs/ticket-11-slice-44-stimpack-close-combat-precision-2026-08-26.md`. The rolling forecast is about 50 further slices.

Graph-selected Slice 45 is now frozen as a denominator/relationship slice rather than a premature runtime promotion. Current Part9 and Marine Parts8/12 prove Bayonet is a non-Specialist Unit-wide Strike replacement, so the queued mixed-carrier assumption is replaced by explicit forbidden paths. The deep denominator binds 6/9 initial roster, arbitrary legal live count, Supply tiers, roster/active/destroyed ledger, Engagement Graph, Fighting/Supporting ranks, Unit-wide RoA and attack pool; casualty, geometry, loadout, status, marker or history drift rejects the old plan. The bounded defender has exactly one remaining Marine model. Slice `0a5c8cc5...e60ab`, unchanged catalogue `732fad40...a649` and runtime `7cdcaa4c...acc`, graph `d7d78031...39c2` and denominator `2a08ec8d...7025` retain 421/912 actionable atoms, 491 remaining and 114 display-only across 45 frozen slices. The graph has 5,118 nodes / 19,710 edges, 37 executors, 3 declared state contracts and 34 debts. Focused `10/10`, runtime `10/10`, historical Slice44 `15/15`, foundations 107 reports / 1,094 assertions plus aggregate = 108 / 1,103, and `verify:all` pass. Exact evidence is frozen in `docs/ticket-11-slice-45-marine-multi-model-close-combat-denominator-2026-08-26.md`; rolling forecast is 53 further slices.

Slice 46 promotes the denominator through `authority.marine-multi-model-stimpack-active-v3@3.0.0` and `authority.marine-multi-model-stimpack-close-combat-v2@2.0.0`. Six/nine initial rosters retain arbitrary legal live counts and Supply tiers; the full ledger, Unit-wide Strike/Bayonet replacement, current Fighting/Supporting ranks, attack pool, Stimpack chain and Precision choice domain are hash-bound. Ordinary attacks have no Precision; Stimpack exposes the complete empty-plus-failed-die-subsets-up-to-three domain after committed Hit dice. Casualty or geometry drift invalidates old actions/pending choices. The relationship graph becomes a mandatory Slice freeze gate for source, consumer, state read/write, derivation/invalidation, Judge-test and version paths while remaining derived evidence rather than Rules authority. Slice `2d6214ab...2454`, catalogue `89f9cd56...e6b`, runtime `5f0aac1f...441` and graph `2af2fd17...4c5` retain 421/912 actionable atoms, 491 remaining and 114 display-only across 46 frozen slices. The graph has 5,138 nodes / 19,927 edges, 39 executors, 5 declared state contracts and 34 debts. Focused `13/13`, runtime `10/10`, foundations 108 reports / 1,107 assertions plus aggregate = 109 / 1,116 pass. Exact evidence is frozen in `docs/ticket-11-slice-46-marine-multi-model-stimpack-close-combat-2026-08-26.md`; rolling planning forecast is 54 further slices.

Slice 47 removes Slice46's one-model-defender bound for the exact current-Marine subset. A pure casualty kernel binds prior damage, new Damage Pool, visibility, the live/destroyed ledger and current Engagement Graph; unengaged selection is visibility-limited, while engaged selection applies official non-engaged / engaged-non-B2B / B2B priority and preserves each specific enemy-Unit engagement whenever another candidate exists. Ordinary combat opens a defender casualty domain; Stimpack resolves attacker Precision first and then the defender casualty domain. Apply updates model ledger, Supply, residual damage or Unit destruction and rederives engagement. Precision v3 validates the expanded denominator while delegating only frozen shared dice math to v2. Only Slice47 uses `hybrid_legal_space_v15`. Slice `a52b9b24...12778`, catalogue `cebc6dff...b2fac`, runtime `e115118c...02cb7` and graph `761ce316...21e0` retain 421/912 actionable atoms, 491 remaining and 114 display-only across 47 frozen slices. The graph has 5,157 nodes / 20,090 edges, 40 executors, 7 declared state contracts, 33 debts and zero blocking gaps. Every new rules Slice must append into a new graph hash; old graph and rules-display identities remain immutable. Focused `12/12`, runtime `10/10`, foundations 109 reports / 1,119 assertions plus aggregate = 110 / 1,128, and `verify:all` pass. Exact evidence is frozen in `docs/ticket-11-slice-47-marine-multi-model-casualty-resolution-2026-08-26.md`; rolling planning forecast is about 55 further slices. No Skill/DSH/MuZero/memory/self-play/training promotion is opened.

Slices 48–49 close the three-Unit casualty and Stimpack Precision composition gaps without changing the 421-atom denominator. Slices 50–54 then switch to the corrected strict-completion order and close existing executor state/relationship/Judge/replay contracts for Phase Initiative, Activation Pass, Movement Hold, Assault Hold and Combat Pass. Slice54's RED test found an unsafe public seam in frozen Combat Pass v2, so current rooms use `authority.combat-pass-v3@3.0.0` with exact enumerated-action matching and a fresh Combat first-actor gate, while v2 stays immutable for historical replay. No new atom was added: coverage is now `421 = 122 strict + 80 partial + 219 none`, with `14/42` declared executor contracts and 28 debts. Current slice `e8764972...90f`, catalogue `eea6d9c0...922d`, runtime `1d6b06dd...e26c`, and graph `1847bcd8...c590` contain 5,509 nodes / 20,883 edges. Live official `71/69/48` revalidation forbids repository fallback; focused `12/12`, runtime `10/10`, 116 base reports / 1,191 assertions plus aggregate = 117 / 1,200, full Ticket 11 foundations and `verify:all` pass. Charge/new atoms remain frozen until `42/42` and `421/421` strict. Next is the round/scoring/lifecycle group, beginning with a planning-only Mission Marker Control impact audit.

Slice 55 closes the existing Mission Marker Control family without adding an atom. A RED test proved that frozen v2 accepted forged RuleAtom lineage, forged resolution and extra public fields; Authority integration also proved diagnostics are outside executable action identity. Current rooms therefore use `authority.mission-marker-control-v3@3.0.0`, which exact-matches the server-enumerated action and only then delegates frozen v2 transition semantics. The same 22 atoms move none→strict, so current coverage is `421 = 144 strict + 80 partial + 197 none`, contracts are `15/42`, and 27 contracts remain. Slice `fdb44c36...d6662`, catalogue `d7ebb1f6...f908`, runtime `7ba03eba...8f55`, and graph `4a8ea539...331a` contain 5,670 nodes / 21,091 edges. Five-marker control, Supply/coherency/exclusion rules, protected writes, graph gaps, LegalSpace→Preview→Confirm→Apply, Ed25519 replay after HMAC rotation, historical v2 display and live official `71/69/48` with repository fallback false all pass. Focused `12/12`, runtime `10/10`, 117 base reports / 1,203 assertions plus aggregate = 118 / 1,212, full Ticket 11 foundations and `verify:all` pass. Slice 56 first audits the directly downstream VP-scoring executor; its possible `+12 strict` effect is planning-only.

Slice 56 closes those 12 existing Victory Point scoring atoms without adding an atom. A RED test proved frozen v1 accepted caller-forged lineage, resolution/diagnostic content and extra public fields. Current rooms use `authority.victory-point-scoring-v2@2.0.0`, which exact-matches current enumeration before delegating frozen v1 transition semantics; v1 source SHA-256 `2f0eb046...ee3af` and historical catalogue/runtime/display dependencies remain exact. The five-marker affinity fixture awards player 1 four VP and player 2 three VP, uses a zero-loss Supply ledger and simultaneously updates `2/1 → 6/4`, while Apply writes only scores/history/Cleanup progress/log. Coverage is `421 = 156 strict + 80 partial + 185 none`, contracts `16/42`, with 26 missing and 265 existing atoms still non-strict. Slice `d29118ef...ba2c`, catalogue `23512e7e...a5a`, runtime `d29dc215...bcfe`, and graph `383a9fdb...4844` contain 5,767 nodes / 21,241 edges. Live official `71/69/48` plus Hold Position revalidation forbids repository fallback; focused `12/12`, runtime `10/10`, 118 base reports / 1,215 assertions plus aggregate = 119 / 1,224, complete Ticket 11 foundations and `verify:all` pass. Slice 57 planning audits the directly downstream two-atom end-game check; `158/80/183`, contracts `17/42`, and 25 missing are projections only.

Slice 57 closes those two existing Hold Position end-game atoms without adding an atom. A RED test proved frozen v1 accepted caller-forged lineage, diagnostics and extra public fields. Current rooms use `authority.hold-position-end-game-check-v2@2.0.0`, which exact-matches current enumeration before delegating frozen v1 transition semantics; v1 source SHA-256 `818368bf...1656` and all historical identities/displays remain exact. The bounded transition proves lead 9 continues to End-of-Round Effects, lead 10 terminates for either seat, terminal LegalSpace is empty, and protected board/piece/score/source/setup/Supply/VP-history state cannot change. Coverage is `421 = 158 strict + 80 partial + 183 none`, contracts `17/42`, with 25 missing and 263 existing atoms still non-strict. Slice `d74733ad...5b21`, catalogue `87cd0663...b181`, runtime `ecb2e900...cf64`, and graph `db04b18a...6b57` contain 5,809 nodes / 21,342 edges. Live Core plus Firestore `71/69/48` and Hold Position revalidation forbid repository fallback; focused `12/12`, runtime `10/10`, 119 base reports / 1,227 assertions plus aggregate = 120 / 1,236, complete Ticket 11 foundations and `verify:all` pass. The next dependency group is End-of-Round Effects v2/v3/v4 plus shared Cleanup consumers; future slices must report contract and atom movement separately.

Slice 58 closes the current End-of-Round Effects group without adding an atom. A RED test proved frozen v4 accepted caller-forged lineage; strict-freeze policy keeps v2/v3/v4 source byte-exact and introduces `authority.end-of-round-effects-v5@5.0.0`, which re-enumerates and exact-matches the public action. Current latest-official dependencies differ from frozen v3/v4, so v5 owns current Optical Flare/Stimpack paths while empty queues retain the valid v2 path; old v3/v4 reject current data and remain historical-display/replay only. Status, markers and non-lethal damage persist until separate Cleanup. Three executor contracts close, but shared consumers mean only two atoms become strict and one none atom becomes partial: `421 = 160 strict + 81 partial + 180 none`, contracts `20/42`, 22 missing and 261 existing atoms non-strict. Slice `4b23af86...014`, catalogue `47f128f3...7dd9`, runtime `ec37637b...b089`, graph `52f9ad4f...aff1` with 5,867 nodes / 21,510 edges, and action schema v18 are frozen. Live Core plus Firestore `71/69/48`, Part 11, Academy and Terran Armed Forces forbid repository fallback. Focused `4/4 + 12/12`, runtime `10/10`, 120 base reports / 1,239 assertions plus aggregate = 121 / 1,248, foundation prefix/suffix and `verify:all` pass. A cumulative rerun also corrected historical evidence projection so per-run Ed25519-key-bound state hashes are verified within their run but are not misclassified as deterministic cross-key rules hashes. Cleanup v2/v3/v4 is the next direct dependency group; its impact remains planning-only until the complete audit passes.

Slices 59–61 close current Cleanup Refresh, Determine Initiative and Start of Round contracts in dependency order without adding RuleAtoms. Historical implementations and old-rules displays remain byte-frozen while current versions bind the latest official data, exact upstream history, re-enumerated actions, Chance commitments where required, protected writes and cross-HMAC Ed25519 replay. After Slice61 the established round chain reaches Movement with a hash-bound `RoundSupplyState` and a still-explicit `PhaseFirstActorChoice`: `421 = 186 strict + 78 partial + 157 none`, contracts `25/42`, with 17 missing. Slice61 `3a81f7d6...b012b`, catalogue `70f8a9b7...18529`, runtime `b4a63b98...c1a99` and graph `62e89408...ee5e` are frozen under action schema v19.

Slice 62 closes current Reserve Deploy over thirty existing atoms. Public RED tests prove frozen v1 accepts a forged Start Supply hash and forged phase marker provenance; v1 remains immutable, while `authority.reserve-deploy-v2@2.0.0` binds the exact Start v2 resolution/log, Phase Initiative choice/log and a contiguous Supply hash chain across multiple current Deploy/Disengage mutations. It exact-matches a fresh v2 domain/action before an explicit adapter invokes the frozen v1 geometry/transition kernel; `silentCompatibilityUsed=false`. Two-seat sequential deployments, protected state, forged lineage rejection, four-receipt replay after HMAC rotation, tamper rejection and live Core/P2P/Firestore `71/69/48` with repository fallback false pass. Coverage is `421 = 216 strict + 67 partial + 138 none`, contracts `26/42`, with 16 missing and 205 existing atoms non-strict. Slice `95d7d170...f378`, catalogue `702434b3...d4e1`, runtime `f8cae053...b27c` and graph `afd54054...a2f8` (`6,328` nodes / `22,319` edges) use action schema v20. Exact evidence is in `docs/ticket-11-slice-62-existing-reserve-deploy-contract-closure-2026-08-28.md`.

Slice 63 closes current Standard Move over ten existing atoms. Dependency and impact ordering selects it before Disengage and Stimpack Move because both consume its movement semantics and all ten target atoms were already partial; closing it makes all ten strict. Public RED proves frozen `authority.standard-move-v1@1.0.0` accepts a forged Movement marker holder and forged Start Supply hash. Current rooms use `authority.standard-move-v2@2.0.0`, which binds current Start v2, Phase Initiative v1 and contiguous Supply provenance, exact-matches fresh domains/actions, and preserves v1 only as an explicit frozen geometry/transition semantic kernel. Unit size is part of the public contract: one Marine uses Speed 7, multiple Marines use Speed 4, and the domain fixes every model start plus exactly `modelCount - 1` placements. Coverage is `421 = 226 strict + 57 partial + 138 none`, contracts `27/42`, with 15 missing and 195 existing atoms non-strict. Slice `0ec04b98...ddea`, catalogue `c437d7ef...d9733`, runtime `9df3c61f...1a33` and graph `8c9585d5...973b` (`6,414` nodes / `22,469` edges) use action schema v21. Public `4/4`, graph `6/6`, comprehensive `13/13`, runtime `10/10` and aggregate 125 base reports / 1,311 assertions plus aggregate = 126 / 1,320 pass before the full gates. Exact evidence is in `docs/ticket-11-slice-63-existing-standard-move-contract-closure-2026-08-28.md`.

Slice 84 closes the first general terrain/line-of-sight primitive group under a deliberately bounded geometry authority. `official-terrain-los-data-bundle-v1` binds 26 current official Unit profiles, 25 printed Sizes and the unchanged Core PDF. `authority.terrain-los-rules-v1@1.0.0` promotes 19 atoms covering setup footprints/openings, independent movement/sight permissions, Size 0–1 versus Size 2+ Leading Model transit, endpoint overlap, Blocking Terrain, Full/Direct/independent cover, top-down surface exclusion, the mutual Size 3+ dead zone, Close Quarters, and visibility. Round bases and axis-aligned rectangles require either a complete barrier proof or an explicit clear base-point witness; unsupported diagonal traces fail closed. Current slice `dfe74426...687f`, catalogue `f3a0170b...438a`, runtime `b61a6aac...e324`, and graph `0c5e5130...d0c` bind `568/344/114` atoms and 53/53 state contracts. Focused `30/30`, runtime `10/10`, and aggregate `10/10` pass. Elevation/effective-Size stacking, special terrain kinds/Access Points, and arbitrary base shapes remain Slices 85–87.

Slice 86 closes the exact 13-atom special-terrain assignment from route v2. `authority.special-terrain-rules-v1@1.0.0` binds a complete content-hashed terrain/Access Point agreement, executes Access Point elevation changes and coherency, Size 1 Mid Ground Ramps, derived Impassable Terrain, ordinary Size 0–1 transit, Size 2 Grass movement/standard-Cover LoS/permanent removal, and Leading Model Gap reuse. Frozen Slice 82/83/84 kernels remain the Gap, Flying and ordinary terrain/LoS authorities. Removed Grass remains in the immutable setup audit but leaves active geometry. Current slice `99454bd0...8059`, catalogue `da040b3e...3622`, runtime `f429e976...e016`, and graph `92362a43...497` bind `591/321/114` atoms and 55/55 state contracts. Focused `30/30`, runtime `10/10`, aggregate `10/10`, and route `10/10` pass. Source lock `1adbdb65...57a1` was not refreshed; arbitrary bases and general model/measurement/coherency placement remain Slice 87.

Slice 87 closes the exact 21-atom model/base geometry assignment from route v2. `official-model-base-geometry-data-bundle-v1` binds the unchanged locked P2P records for all 26 current Units: 25 round bases and Hydralisk's rectangular `40×100MM` base. `authority.model-base-geometry-rules-v1@1.0.0` owns base-only measurement, unrestricted premeasurement, `Within`/`Wholly Within`, full multi-model placement and Coherency Link validation, Access Point links, placement-casualty proof, mission capability, wobbly-position agreement and Leading Model nomination expiry. Current slice `df60e9e...328`, catalogue `e19ea565...59a0`, runtime `d1dcbe60...2ddc`, and graph `0a5679f7...fa8` bind `612/300/114` atoms and 56/56 state contracts. Focused `34/34`, runtime `10/10`, and aggregate `10/10` pass. Source lock `1adbdb65...57a1` was not refreshed; Slices 88–111 retain exactly 300 actionable atoms.

Slice 88 closes the exact 15-atom player/control relationship assignment from route v2. `official-player-control-relationship-data-bundle-v1` binds the unchanged locked Core PDF plus exact Part 2 and Part 11 prose records. `authority.player-control-relationship-rules-v1@1.0.0` derives Active/Controlling Player authority, legal owner versus transferred controller, arbitrary team Friendly/Enemy relationships, Friendly rule/attack constraints and content-hashed specific-over-general precedence. Equal-specificity contradictions and client-supplied controller/relationship/precedence truth fail closed. Current slice `4798bbe5...9987`, catalogue `50135173...4536`, runtime `b3e9b398...901f`, and graph `b74c7a91...494` bind `627/285/114` atoms and 57/57 state contracts. Focused `35/35`, runtime `10/10`, and aggregate `10/10` pass. Source lock `1adbdb65...57a1` was not refreshed; Slices 89–111 retain exactly 285 actionable atoms.

Slice 89 closes the exact 18-atom dice/test/modifier assignment from route v2. `official-dice-test-modifier-data-bundle-v1` binds the unchanged source lock, Core PDF and exact Part 3/Part 11 prose records. `authority.dice-test-modifier-rules-v1@1.0.0` owns named-source stacking, Buff/Debuff modifier direction, target-number clamping, Null capability, Characteristic/Attribute tests, value generation, reroll scope/replacement and physical invalid-die handling. The two-stage Authority reroll path commits the initial roll before player choice and commits a separate reroll chance only when chosen. Current slice `ea7f1b10...b632`, catalogue `af887ff1...1a8f`, runtime `81fce5be...8df4`, and graph `9e9b4898...0dc6` bind `645/267/114` atoms and 58/58 state contracts. Focused `49/49`, runtime `10/10`, and aggregate `10/10` pass. Source lock `1adbdb65...57a1` was not refreshed; Slices 90–111 retain exactly 267 actionable atoms.

## Verification status

Ticket 11A is command-verified: `npm --prefix starcraft-tmg-level3 run verify:ticket-11-authority` passes its fixed `15/15` authority denominator and the HTTP authority boundary. The immutable report is `build/ticket-11-authority-v2/report.json`. The run exercises a local SQLite WAL store; the PostgreSQL Adapter is checked against the same declared atomic/CAS contract but is not connected to a production database. Ticket 11 remains open because 645/1,026 RuleAtoms have executable Rule/LegalSpace/Judge/replay evidence; equivalently, 645/912 actionable atoms are executable (`70.7%`). The remaining 267 actionable atoms include lifecycle/status/roster/mission primitives and interaction families. All 58 current executors have declared strict state contracts for their bounded denominators, but that does not make the incomplete catalogue or unimplemented combinations complete. Slice 78's template procedure remains production-quarantined where an official carrier or complete physical geometry is unavailable. Even a future 912/912 atomic count still requires combination, complete LegalSpace, transition/replay, version-freeze and interaction closure. Browser/App acceptance, complete Agent/Prompt/Memory/Skill/self-play/training Harness behavior and production integration are also pending.

The current runtime is `81fce5be2083d1c54375f1c358b7c2653b7c62af6226c9dc5808616c8b828df4`; every earlier runtime and old-rules display identity remains required. The current foundation evidence is 153 base reports / 1,782 assertions, plus aggregate for 154 reports / 1,792 assertions. This proves exclusion and fail-closed behavior only for the current 645-atom bounded subsets, 58 declared executor state contracts and the exact supported composition. It does not prove complete LegalSpace, arbitrary unsupported units/deployments/weapons/abilities, official BT/FT carrier/physical geometry availability, broader lifecycle/status/roster/mission interactions, Army Elimination/final scoring, browser/device acceptance, complete Harness behavior or production readiness.

Implemented in the fixed development root; Ticket 11A and the explicitly listed current Ticket 11B subsets below are command-verified:

- `starcraft-tmg-level3/packages/authoritative-engine/referee-crypto-v1.mjs`
- `starcraft-tmg-level3/packages/authoritative-engine/transition-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-assault-hold-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-assault-hold-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-disengage-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-disengage-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-disengage-casualty-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-disengage-casualty-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-executable-rule-runtime-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-goliath-charge-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-impact-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-impact-after-charge-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-impact-profile-bundle-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-marine-move-geometry-kernel-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-marine-optional-stimpack-move-executor-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-marine-optional-stimpack-move-rule-slice-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-total-damage-reaction-kernel-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-optical-flare-ranged-consumer-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-medic-life-support-reaction-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-life-support-damage-reaction-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-sidearm-pinpoint-effect-kernel-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-sidearm-pinpoint-ranged-batch-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-sidearm-pinpoint-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-combat-effect-denominator-v3.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-mission-marker-control-kernel-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-mission-marker-control-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-mission-marker-control-executor-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-mission-marker-control-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-supply-loss-ledger-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-supply-loss-combat-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-victory-point-scoring-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-victory-point-scoring-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-hold-position-end-game-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-hold-position-end-game-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-end-of-round-effects-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-end-of-round-effects-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-start-of-round-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-start-of-round-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-phase-initiative-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-phase-initiative-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-engagement-graph-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-engagement-graph-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-combat-pass-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-combat-pass-executor-v2.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-combat-pass-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-elevated-engagement-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-close-combat-attack-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-close-combat-attack-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-close-ranks-combat-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-close-ranks-combat-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-multi-model-close-ranks-combat-executor-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-multi-model-close-ranks-rule-slice-v1.mjs`
- `starcraft-tmg-level3/packages/authoritative-engine/replay-projection-verifier-v1.mjs`
- `starcraft-tmg-level3/packages/room-store/room-store-v1.mjs`
- `starcraft-tmg-level3/packages/room-store/sqlite-room-store-v1.mjs`
- `starcraft-tmg-level3/packages/room-store/postgres-room-store-v1.mjs`
- `starcraft-tmg-level3/packages/room-runtime/in-memory-room-v1.mjs`
- `starcraft-tmg-level3/packages/http-adapter/handler-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-ticket-11-authority-v2.mjs`
- `starcraft-tmg-level3/scripts/verify-official-assault-hold-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-disengage-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-disengage-casualty-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-executable-rule-runtime-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-impact-after-charge-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-assault-run-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-marine-optional-stimpack-move-rule-slice-v2.mjs`
- `starcraft-tmg-level3/scripts/verify-official-life-support-damage-reaction-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-sidearm-pinpoint-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-phase-initiative-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-combat-pass-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-elevated-engagement-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-close-combat-attack-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-close-ranks-combat-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-multi-model-close-ranks-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-victory-point-scoring-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-hold-position-end-game-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-end-of-round-effects-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-start-of-round-rule-slice-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-ticket-11-rule-atom-foundation-aggregate-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-authoritative-transition-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-authoritative-room-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-http-adapter-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/contracts-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/mode-capability-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/prompt-assembly-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/session-runtime-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/http-handler-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/worldbook-registry-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/character-card-v2-adapter-v1.mjs`
- `starcraft-tmg-level3/packages/product-composition/character-session-factory-v1.mjs`
- `starcraft-tmg-level3/content/characters/kerrigan-primal-v1.mjs`
- `starcraft-tmg-level3/content/characters/kerrigan-worldbook-catalog-v1.mjs`
- `starcraft-tmg-level3/content/characters/original-tactical-adjutant-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-kerrigan-role-agent-v1.mjs`
- `starcraft-tmg-level3/packages/character-agent/openai-compatible-provider-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-direct-provider-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-character-worldbook-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/source-registry-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-latest-data-binding-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-command-center-adapter-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-p2p-alias-index-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-gameplay-data-bundle-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/official-mission-setup-binding-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/core-clause-candidate-denominator-v1.mjs`
- `starcraft-tmg-level3/packages/rule-atoms/official-faq-core-linkage-v1.mjs`
- `starcraft-tmg-level3/content/source-registry-v1.mjs`
- `starcraft-tmg-level3/packages/source-data/legacy-pack-adapter-v1.mjs`
- `starcraft-tmg-level3/packages/localization/translation-sidecar-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-source-translation-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-latest-data-binding-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-command-center-adapter-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-p2p-alias-precedence-v1.mjs`
- `starcraft-tmg-level3/content/official-p2p-alias-binding-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-core-clause-candidate-denominator-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-official-faq-core-linkage-v1.mjs`
- `starcraft-tmg-level3/content/official-faq-core-link-binding-v1.mjs`
- `starcraft-tmg-level3/content/localization/zh-cn-core-v1.mjs`
- `starcraft-tmg-level3/packages/localization/source-localization-runtime-v1.mjs`
- `starcraft-tmg-level3/packages/localization/http-handler-v1.mjs`
- `starcraft-tmg-level3/packages/product-composition/source-localization-factory-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-source-localization-runtime-v1.mjs`
- `starcraft-tmg-level3/packages/skill-generation/contracts-v1.mjs`
- `starcraft-tmg-level3/packages/skill-generation/offline-arm-adapter-v1.mjs`
- `starcraft-tmg-level3/scripts/verify-offline-skill-arms-v1.mjs`
- HTTP room creation, join, projection, LegalSpace, preview, confirmed apply, and replay routes
- deterministic `occurredAt` normalization inside the authoritative Adapter, without modifying legacy rule code
- four isolated role sessions, server-configured Agent HTTP sessions, secure-transport BYOK gate, direct-Provider boundary, sealed prompt receipts and opponent-only decision/preview contract
- HTTPS/configured-model Provider routing, bounded structured JSON, no internal retry, complete body timeout, safe failure receipts, and receipt/profile/output binding in Harness traces
- nine-book era/context catalogue, cross-era/spoiler/knowledge rejection, bounded activation receipts, server-configured character/worldbook selection, sealed/external Character Card V2 JSON paths and original fallback binding
- source-scope and rights separation, content-bound snapshot receipts, legacy inventory lineage/audit, community/derived separation, glossary/provider-bound translation drafts, human-review sidecars, and stale-source canonical fallback
- shared source/localization runtime, server-selected Provider profiles, administrator-only cost-bearing translation requests, principal-bound review, ordinary-client draft hiding, and credential rejection at the HTTP seam
- pinned DSH/direct-control execution-arm parity, isolated capability manifest, exact-one candidate emission, candidate/receipt integrity, budget accounting, credential scanning, and zero-promotion defaults

Executed Ticket 11A gate:

```sh
npm --prefix starcraft-tmg-level3 run verify:ticket-11-authority
```

This command covers the fixed 15-item Ticket 11A authority-kernel denominator plus the HTTP authority boundary and passed `15/15`. It does not close Ticket 11B's full source-derived RuleAtom coverage gate. Legacy regression, Expo/Battle Lab parity, browser, and device gates are added only when their bounded integration increment begins. No production schema migration, public server, APK, Provider, DSH runtime, or training pipeline was changed by this verification.
