# Slice 173 — actual before/after counterexample repair

Ticket18 remains 1/8, project16/22, formal five-piece set0/5. This is work
inside Slice173, not another Slice. No official-source refresh or Codex agents.

## Frozen comparison and first actual result

The five independently identified source defects have 14 source-grounded
literal-reading/comprehension controls. The evaluator sees all 67 claims in
the five packets, but no source quotes, expected answers or editing history.
These controls are known-error regressions, not a secret held-out game exam.
It receives one structure repair only; wrong answers never cause score retry.

Run `rules-v3-45543327938bde810be5`:

- The actual fresh reader detected all five negative controls in the old text.
- Old packet comprehension/fidelity score: **5/14**. This is evidence of
  defects, not successful Skill acceptance.
- Packet002's two claims were actually corrected and passed two fresh
  same-model source reviews. Flying no longer grants universal visibility;
  the Skill no longer invents priority for a conflicting summary table.
- Packet003's editor returned both requested replacement paths but copied
  both defective texts verbatim. The deterministic no-progress check stopped
  the run before more reviews or packet005. This is a valid refusal of an
  ineffective edit, not a formatting failure or a source refresh problem.
- Five physical calls, 518,601 tokens, estimated ¥0.090621. Global known
  lower bound8,804,291 tokens; estimate plus historical reserve¥38.344110.
  Historical unknown-call reserve remains¥28.961350; these are not invoices.
- Terminal report hash:
  `7d6b842ee056f854d9e161ef5d32084ca823198bca4ba754d176293c94cca998`.

## Diagnosed and implemented recovery

The actual saved response is a deterministic regression fixture. Both text
hashes equal the original bad text; exact source quotes exist. Thus the
failure is observed non-execution of the edit, not a hash false positive or
missing source delivery. The model's internal reason remains unknown.

The editor now receives one additional, explicitly identified feedback turn:
the unchanged claim IDs, original response hash/patch and exact official
evidence. This is not another call with the same input or an unbounded retry.
Another unchanged result still stops. Unflagged claims remain byte-identical,
fresh source reviews and the 14 after-controls are still mandatory.

The first saved failure was reproduced RED, then six focused recovery checks
passed with injected correction text. Twenty external-finding/evaluation
workflow checks, ten v3 groups and twelve continuation checks pass. Injection
proves control flow only; the actual recovery result is recorded separately.

Continuation `rules-v3-6854c9dc1f754556dafd` is started. Five exact-input
outputs/controls can be reused, including packet002 and the unchanged
packet003 response, without copying final acceptance or Provider attempts.
The original¥5/100-call/8M-token/three-hour budget subtracts prior usage and
retains the original start. Original reviews, bad text and paid receipts stay.

## Acceptance still required

1. Actual003/005 correction and fresh reviews.
2. Actual after-controls and independent inspection of final claims.
3. The dependency gate must recompute scores and resolve real Provider
   receipts from SQLite; self-reported green results cannot pass.
4. Remaining32 packets, full105-case overall exam, factions, both matchups,
   real Room Harness/strategy assessment and replay/reflection/version upgrade.

The skills used here enforce full context, external counterexamples,
evidence-bound local edits, persistent feedback and no silent promotion.
`ctx2skillLoopUsed=true`, `harnessLoopUsed=true`, `trainingTruth=false`.
